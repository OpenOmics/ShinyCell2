#' Write code for ui.R
#'
#' @rdname wrUIlib
#' @export wrUIlib
#'
wrUIlib <- function() {
  glue::glue(
    'library(shiny) \n',
    'library(shinyhelper) \n',
    'library(data.table) \n',
    'library(Matrix) \n',
    'library(DT) \n',
    'library(magrittr) \n',
    'library(colourpicker) \n',
    '\n'
  )
}

#' Write code for ui.R
#'
#' @rdname wrUIload
#' @export wrUIload
#'
wrUIload <- function(prefix) {
  glue::glue(
    '{prefix}conf = readRDS("./{prefix}conf.rds")\n',
    '{prefix}def  = readRDS("./{prefix}def.rds")\n',
    '{prefix}meta = readRDS("./{prefix}meta.rds")\n',
    '\n'
  )
}

#' Write code for ui.R
#'
#' @rdname wrUIloadT1
#' @export wrUIloadT1
#'
wrUIloadT1 <- function(prefix) {
  glue::glue(
    '{prefix}bw    = readRDS("./{prefix}bw.rds")\n',
    '{prefix}bwGrp = gsub("{prefix}bw_", "", dir(pattern = "{prefix}bw_"))\n',
    '\n'
  )
}

#' Write code for ui.R
#'
#' @rdname wrUIloadS1
#' @export wrUIloadS1
#'
wrUIloadS1 <- function(prefix) {
  glue::glue(
    '{prefix}image = readRDS("./{prefix}image.rds")\n',
    'if(is.null({prefix}image$slide_display_names)) {prefix}image$slide_display_names <- {prefix}image$slide_names\n',
    '\n'
  )
}

#' Write code for ui.R
#'
#' @rdname wrUIpre
#' @export wrUIpre
#'
wrUIpre <- function(title, ganalytics) {
  if(!is.na(ganalytics)){
    ga = 'tags$head(includeHTML(("google-analytics.html"))),'
  } else {
    ga = ''
  }
  glue::glue(
    '\n',
    '\n',
    '\n',
    '### Start server code \n',
    'shinyUI(fluidPage( \n',
    '  ### Global theme: fonts, colours, layout + light/dark support\n',
    '  tags$head(\n',
    '    tags$link(rel = "preconnect", href = "https://fonts.googleapis.com"),\n',
    '    tags$link(rel = "preconnect", href = "https://fonts.gstatic.com", crossorigin = NA),\n',
    '    tags$link(rel = "stylesheet",\n',
    '              href = "https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"),\n',
    '    tags$style(HTML(r"(\n',
    '      /* ---- Design tokens ---------------------------------------------- */\n',
    '      :root {{\n',
    "        --sc-font: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;\n",
    '        --sc-bg: #f4f6f9;\n',
    '        --sc-surface: #ffffff;\n',
    '        --sc-surface-2: #eef1f6;\n',
    '        --sc-text: #1f2933;\n',
    '        --sc-text-muted: #5b6675;\n',
    '        --sc-border: #d7dce3;\n',
    '        --sc-primary: #2c6e9b;\n',
    '        --sc-primary-hover: #235a80;\n',
    '        --sc-primary-contrast: #ffffff;\n',
    '        --sc-accent: #1b7837;\n',
    '        --sc-danger: #c0392b;\n',
    '        --sc-radius: 10px;\n',
    '        --sc-radius-sm: 6px;\n',
    '        --sc-shadow: 0 1px 3px rgba(16,24,40,.08), 0 1px 2px rgba(16,24,40,.06);\n',
    '        --sc-shadow-lg: 0 6px 18px rgba(16,24,40,.10);\n',
    '      }}\n',
    '      body.sc-dark {{\n',
    '        --sc-bg: #0f1620;\n',
    '        --sc-surface: #18212e;\n',
    '        --sc-surface-2: #1f2a39;\n',
    '        --sc-text: #e6ecf4;\n',
    '        --sc-text-muted: #9aa7b8;\n',
    '        --sc-border: #2c3a4d;\n',
    '        --sc-primary: #4a9fd6;\n',
    '        --sc-primary-hover: #5fb0e4;\n',
    '        --sc-primary-contrast: #0f1620;\n',
    '        --sc-accent: #56c271;\n',
    '        --sc-danger: #e2675a;\n',
    '        --sc-shadow: 0 1px 3px rgba(0,0,0,.45), 0 1px 2px rgba(0,0,0,.4);\n',
    '        --sc-shadow-lg: 0 8px 24px rgba(0,0,0,.5);\n',
    '      }}\n',
    '\n',
    '      /* ---- Base ------------------------------------------------------- */\n',
    '      body, .shiny-bound-output {{\n',
    '        font-family: var(--sc-font);\n',
    '        background-color: var(--sc-bg);\n',
    '        color: var(--sc-text);\n',
    '        font-size: 15px;\n',
    '        line-height: 1.5;\n',
    '        transition: background-color .25s ease, color .25s ease;\n',
    '      }}\n',
    '      body {{ overflow-x: hidden; }}   /* guard against incidental horizontal scroll */\n',
    '      .container-fluid {{ max-width: 1360px; margin: 0 auto; padding: 0 4px 48px; }}\n',
    '      h1, h2, h3, h4, h5 {{ font-family: var(--sc-font); color: var(--sc-text); font-weight: 600; }}\n',
    '      h2 {{ font-size: 26px; font-weight: 700; letter-spacing: -0.01em; }}\n',
    '      h4 {{ font-size: 17px; margin-top: 4px; margin-bottom: 14px; color: var(--sc-text); }}\n',
    '      a {{ color: var(--sc-primary); }}\n',
    '      a:hover, a:focus {{ color: var(--sc-primary-hover); }}\n',
    '      strong {{ color: var(--sc-text); }}\n',
    '      hr {{ border-top: 1px solid var(--sc-border); }}\n',
    '\n',
    '      /* ---- App header (title + theme switch) ------------------------- */\n',
    '      /* Flat full-width title band with a subtle static fill (no rounded\n',
    '         card). Stretches edge-to-edge and reads cleanly in both themes. */\n',
    '      .sc-app-header {{\n',
    '        display: flex; align-items: center; justify-content: space-between;\n',
    '        gap: 16px; flex-wrap: wrap;\n',
    '        margin-left: calc(50% - 50vw); margin-right: calc(50% - 50vw);\n',
    '        /* align inner content to the centered 1360px column, clamped so it\n',
    '           never goes negative on narrow viewports */\n',
    '        padding: 18px max(4px, calc(50vw - 680px + 4px));\n',
    '        background: var(--sc-surface-2);\n',
    '        border-bottom: 1px solid var(--sc-border);\n',
    '      }}\n',
    '      .sc-app-titles {{ min-width: 0; }}\n',
    '      .sc-app-title {{\n',
    '        margin: 0; font-size: 26px; font-weight: 700; letter-spacing: -0.02em;\n',
    '        color: var(--sc-text); line-height: 1.15;\n',
    '      }}\n',
    '      .sc-app-subtitle {{\n',
    '        margin: 3px 0 0; font-size: 14px; font-weight: 500; color: var(--sc-text-muted);\n',
    '      }}\n',
    '\n',
    '      /* ---- Top navigation bar (full page width) ---------------------- */\n',
    '      .navbar {{\n',
    '        border: none; border-radius: 0; min-height: 0;\n',
    '      }}\n',
    '      .navbar-default {{\n',
    '        position: sticky; top: 0; z-index: 1030;\n',
    '        /* Full-bleed to both viewport edges. The bar lives inside the centered\n',
    '           1360px .container-fluid, so we stretch it out with symmetric\n',
    '           viewport-relative margins. body{{overflow-x:hidden}} absorbs the\n',
    '           scrollbar-width overshoot, so no horizontal page scroll appears. */\n',
    '        margin-left: calc(50% - 50vw);\n',
    '        margin-right: calc(50% - 50vw);\n',
    '        margin-bottom: 22px;\n',
    '        background: var(--sc-surface);\n',
    '        border: none;\n',
    '        border-bottom: 1px solid var(--sc-border);\n',
    '        box-shadow: var(--sc-shadow);\n',
    '      }}\n',
    '      /* App renders under Bootstrap 5 / bslib: .navbar > .container-fluid is a\n',
    '         flexbox. Align its inner content to the SAME centered 1360px column as\n',
    '         the title band and the main content, so all three left edges line up. */\n',
    '      .navbar > .container-fluid,\n',
    '      .navbar-default > .container-fluid {{\n',
    '        width: 100%; margin: 0;\n',
    '        padding: 0 max(4px, calc(50vw - 680px + 4px));\n',
    '        display: flex; flex-wrap: nowrap; align-items: center;\n',
    '        justify-content: flex-start;\n',
    '      }}\n',
    '      /* Empty brand area (title lives in the header band, not here) */\n',
    '      .navbar .navbar-header {{ display: none; }}\n',
    '      /* Page links flush with the content column, forced onto a single line. */\n',
    '      .navbar .navbar-nav {{\n',
    '        float: none;\n',
    '        display: flex; flex-direction: row; flex-wrap: nowrap;\n',
    '        margin: 0; gap: 0;\n',
    '        font-weight: 500; font-size: 14px;\n',
    '      }}\n',
    '      /* First link sits flush at the column edge (cancel its inner padding) */\n',
    '      .navbar .navbar-nav > li:first-child > a {{ padding-left: 0; }}\n',
    '      /* Render tabs as flat text links, not buttons */\n',
    '      .navbar-default .navbar-nav > li {{ float: none; }}\n',
    '      .navbar-default .navbar-nav > li > a {{\n',
    '        color: var(--sc-text-muted);\n',
    '        background: transparent;\n',
    '        padding: 14px 9px;\n',
    '        line-height: 1.2;\n',
    '        white-space: nowrap;\n',
    '        letter-spacing: 0;\n',
    '        border: none;\n',
    '        border-bottom: 2px solid transparent;\n',
    '        transition: color .15s ease, border-color .15s ease;\n',
    '      }}\n',
    '      .navbar-default .navbar-nav > li > a:hover,\n',
    '      .navbar-default .navbar-nav > li > a:focus {{\n',
    '        color: var(--sc-primary); background: transparent;\n',
    '        border-bottom-color: color-mix(in srgb, var(--sc-primary) 45%, transparent);\n',
    '      }}\n',
    '      .navbar-default .navbar-nav > .active > a,\n',
    '      .navbar-default .navbar-nav > .active > a:hover,\n',
    '      .navbar-default .navbar-nav > .active > a:focus {{\n',
    '        color: var(--sc-primary);\n',
    '        background: transparent;\n',
    '        font-weight: 600;\n',
    '        border-bottom-color: var(--sc-primary);\n',
    '      }}\n',
    '      /* Smaller, lighter qualifier word inside a tab label (e.g. the\n',
    '         "spatial"/"dimred" part of a "Zoom" tab) to keep labels compact. */\n',
    '      .nav-sub {{\n',
    '        font-size: 11px; font-weight: 600; text-transform: uppercase;\n',
    '        letter-spacing: .04em; margin-left: 5px; color: #9b2226;\n',
    '      }}\n',
    '      /* Lighter, warmer red so the sub-label stays legible on the dark theme */\n',
    '      body.sc-dark .nav-sub {{ color: #f08a84; }}\n',
    '\n',
    '      /* ---- Cards / column dividers ----------------------------------- */\n',
    '      /* Recolour the legacy inline "border-right: 2px solid black" dividers */\n',
    '      div[style*="border-right"] {{\n',
    '        border-right: 1px solid var(--sc-border) !important;\n',
    '        padding-right: 18px;\n',
    '      }}\n',
    '      /* Plots and their legends share a user-chosen background (white by\n',
    '         default). Collapse the inline-image baseline gap and any margin so no\n',
    '         sliver of the page background shows between a plot and the legend\n',
    '         stacked directly beneath it. */\n',
    '      .shiny-plot-output {{ display: block; margin: 0; }}\n',
    '      .shiny-plot-output img {{ display: block; }}\n',
    '\n',
    '      /* ---- Form controls --------------------------------------------- */\n',
    '      label, .control-label, .shiny-input-container > label {{\n',
    '        font-weight: 600; color: var(--sc-text); font-size: 14px; margin-bottom: 5px;\n',
    '      }}\n',
    '      .form-control, .selectize-input, textarea {{\n',
    '        background-color: var(--sc-surface) !important;\n',
    '        color: var(--sc-text) !important;\n',
    '        border: 1px solid var(--sc-border) !important;\n',
    '        border-radius: var(--sc-radius-sm) !important;\n',
    '        box-shadow: none !important;\n',
    '        transition: border-color .15s ease, box-shadow .15s ease;\n',
    '      }}\n',
    '      .form-control:focus, .selectize-input.focus, textarea:focus {{\n',
    '        border-color: var(--sc-primary) !important;\n',
    '        box-shadow: 0 0 0 3px color-mix(in srgb, var(--sc-primary) 25%, transparent) !important;\n',
    '      }}\n',
    '      .selectize-dropdown {{\n',
    '        background-color: var(--sc-surface); color: var(--sc-text);\n',
    '        border: 1px solid var(--sc-border);\n',
    '        border-radius: var(--sc-radius-sm); box-shadow: var(--sc-shadow-lg);\n',
    '      }}\n',
    '      .selectize-dropdown .active {{ background-color: var(--sc-surface-2); color: var(--sc-text); }}\n',
    '      .selectize-input > .item {{ color: var(--sc-text); }}\n',
    '\n',
    '      /* ---- Buttons ---------------------------------------------------- */\n',
    '      .btn {{\n',
    '        border-radius: var(--sc-radius-sm);\n',
    '        font-weight: 600;\n',
    '        border: 1px solid var(--sc-border);\n',
    '        background-color: var(--sc-surface);\n',
    '        color: var(--sc-text);\n',
    '        box-shadow: none;\n',
    '        transition: background-color .15s ease, color .15s ease, border-color .15s ease, transform .05s ease;\n',
    '      }}\n',
    '      .btn:hover {{ background-color: var(--sc-surface-2); border-color: var(--sc-primary); color: var(--sc-primary); }}\n',
    '      .btn:active {{ transform: translateY(1px); }}\n',
    '      .btn-primary, .btn-default.btn-primary {{\n',
    '        background-color: var(--sc-primary) !important;\n',
    '        border-color: var(--sc-primary) !important;\n',
    '        color: var(--sc-primary-contrast) !important;\n',
    '      }}\n',
    '      .btn-primary:hover {{\n',
    '        background-color: var(--sc-primary-hover) !important;\n',
    '        border-color: var(--sc-primary-hover) !important;\n',
    '      }}\n',
    '      /* Keep download buttons within their grid column so they don\'t crowd the\n',
    '         adjacent "Plot/Legend format:" controls. */\n',
    '      .shiny-download-link {{\n',
    '        display: inline-block; max-width: 100%;\n',
    '        margin-bottom: 6px; margin-right: 12px;\n',
    '        white-space: normal; text-align: left;\n',
    '      }}\n',
    '      /* Add a little breathing room above any row holding download buttons so\n',
    '         they sit clear of the legend / plot figure directly above. */\n',
    '      .row:has(> div > .shiny-download-link),\n',
    '      .row:has(> div > div > .shiny-download-link) {{ margin-top: 14px; }}\n',
    '\n',
    '      /* ---- Sliders (ionRangeSlider) ---------------------------------- */\n',
    '      .irs--shiny .irs-bar, .irs--shiny .irs-handle > i:first-child {{ background-color: var(--sc-primary); }}\n',
    '      .irs--shiny .irs-handle {{ border-color: var(--sc-primary); }}\n',
    '      .irs--shiny .irs-single, .irs--shiny .irs-from, .irs--shiny .irs-to {{\n',
    '        background-color: var(--sc-primary);\n',
    '      }}\n',
    '      .irs--shiny .irs-line {{ background: var(--sc-surface-2); }}\n',
    '      .irs--shiny .irs-min, .irs--shiny .irs-max {{ background: var(--sc-surface-2); color: var(--sc-text-muted); }}\n',
    '\n',
    '      /* ---- Radio / checkbox ------------------------------------------ */\n',
    '      .radio label, .checkbox label, .shiny-options-group label {{ font-weight: 500; color: var(--sc-text); }}\n',
    '\n',
    '      /* ---- Tables ----------------------------------------------------- */\n',
    '      /* Keep DataTables fluid: the widget otherwise computes a natural width\n',
    '         from its content + export buttons that overflows its (narrow) column\n',
    '         and gets clipped at the page edge. Clamp the wrapper and table to the\n',
    '         column width and let long cell text wrap instead of pushing wider. */\n',
    '      .dataTables_wrapper {{ color: var(--sc-text); width: 100%; max-width: 100%; }}\n',
    '      table.dataTable {{ color: var(--sc-text); border-color: var(--sc-border);\n',
    '        width: 100% !important; max-width: 100%; }}\n',
    '      table.dataTable td {{\n',
    '        white-space: normal; word-break: break-word;\n',
    '      }}\n',
    '      /* Keep header labels on a single line (they are short) so they don\'t wrap\n',
    '         to two rows in the narrow table column. Body cells still wrap. */\n',
    '      table.dataTable th {{ white-space: nowrap; }}\n',
    '      table.dataTable thead th {{ border-bottom: 2px solid var(--sc-border); color: var(--sc-text); }}\n',
    '      table.dataTable.stripe tbody tr.odd, table.dataTable tbody tr {{\n',
    '        background-color: var(--sc-surface);\n',
    '      }}\n',
    '      table.dataTable tbody tr:hover {{ background-color: var(--sc-surface-2); }}\n',
    '      .dataTables_wrapper .dataTables_paginate .paginate_button {{\n',
    '        color: var(--sc-text) !important;\n',
    '      }}\n',
    '      .dataTables_wrapper .dt-buttons .dt-button {{\n',
    '        background: var(--sc-surface); border: 1px solid var(--sc-border);\n',
    '        border-radius: var(--sc-radius-sm); color: var(--sc-text);\n',
    '      }}\n',
    '\n',
    '      /* ---- Plot panels ------------------------------------------------ */\n',
    '      /* The plot image carries its own (user-chosen, white-default) background\n',
    '         edge-to-edge, so keep the container neutral white to avoid a themed\n',
    '         sliver showing behind transparent-device plots. */\n',
    '      .shiny-plot-output {{ background: #ffffff; }}\n',
    '\n',
    '      /* ---- Misc ------------------------------------------------------- */\n',
    '      .shiny-output-error-validation {{ color: var(--sc-danger); font-weight: 600; }}\n',
    '      .centerSelect .selectize-input,\n',
    '      .centerSelect .selectize-input input,\n',
    '      .centerSelect .selectize-dropdown .option {{ text-align: center; }}\n',
    '\n',
    '      /* ---- Theme switcher (in the app header, on every page) --------- */\n',
    '      .sc-theme-switch {{\n',
    '        flex: 0 0 auto;\n',
    '        display: inline-flex; align-items: center; gap: 8px;\n',
    '        background: var(--sc-surface); border: 1px solid var(--sc-border);\n',
    '        border-radius: 999px; padding: 5px 12px; box-shadow: var(--sc-shadow);\n',
    '      }}\n',
    '      .sc-theme-switch label {{ margin: 0; font-size: 13px; color: var(--sc-text-muted); font-weight: 600; }}\n',
    '      .sc-theme-switch select {{\n',
    '        font-family: var(--sc-font); font-size: 13px; font-weight: 600;\n',
    '        color: var(--sc-text); background: var(--sc-surface);\n',
    '        border: 1px solid var(--sc-border); border-radius: 999px;\n',
    '        padding: 4px 10px; cursor: pointer; outline: none;\n',
    '      }}\n',
    '    )")),\n',
    '    tags$script(HTML(r"(\n',
    '      (function(){{\n',
    '        function apply(t){{\n',
    "          document.body.classList.toggle('sc-dark', t === 'dark');\n",
    "          try {{ localStorage.setItem('scTheme', t); }} catch(e){{}}\n",
    '        }}\n',
    '        function init(){{\n',
    "          var saved = 'light';\n",
    "          try {{ saved = localStorage.getItem('scTheme') || 'light'; }} catch(e){{}}\n",
    "          var sel = document.getElementById('scThemeSelect');\n",
    '          if (sel) {{\n',
    '            sel.value = saved;\n',
    "            sel.addEventListener('change', function(){{ apply(this.value); }});\n",
    '          }}\n',
    '          apply(saved);\n',
    '        }}\n',
    "        if (document.readyState === 'loading') {{\n",
    "          document.addEventListener('DOMContentLoaded', init);\n",
    '        }} else {{ init(); }}\n',
    '      }})();\n',
    '    )"))\n',
    '  ),\n',
    '\n',
    '  ### App header — title (left) + theme switcher (right), above the nav bar.\n',
    '  ### Rendered once and shared across all tabs, so the theme control is\n',
    '  ### available on every page without overlapping the navigation links.\n',
    '  div(class = "sc-app-header",\n',
    '      div(class = "sc-app-titles",\n',
    '          h1(class = "sc-app-title", "{title}"),\n',
    '          p(class = "sc-app-subtitle", "Interactive single-cell data explorer")),\n',
    '      div(class = "sc-theme-switch",\n',
    '          tags$label("for" = "scThemeSelect", "Theme"),\n',
    '          HTML("<select id=\'scThemeSelect\'><option value=\'light\'>Light</option><option value=\'dark\'>Dark</option></select>"))),\n',
    '  navbarPage(\n',
    '    title = NULL,\n',
    '\n'
  )
}

#' Write code for ui.R
#'
#' @rdname wrUImainA1
#' @export wrUImainA1
#'
wrUImainA1 <- function(prefix, ptsiz) {
  glue::glue(
    '  ### Tab1.a1: Zoom-enable Dimred \n',
    '  tabPanel( \n',
    '    HTML("Zoom<span class=\'nav-sub\'>DimRed</span>"), \n',
    '    h4("Zoom-enable reduced dimensions overlaid with cell info or assay expression"), \n',
    '    "In this tab, users can visualise either cell information and gene expression ",  \n',
    '    "in a zoom-enabled low-dimensional representions plots.", \n',
    '    br(),br(), \n',
    '    fluidRow( \n',
    '      column( \n',
    '        3, fluidRow( \n',
    '          column( \n',
    '            12, selectInput("{prefix}a1dr", "Reduction:", choices = {prefix}def$dimrd, \n',
    '                            selected = {prefix}def$dimrd[1])) \n',
    '        ) \n',
    '      ), # End of column (6 space) \n',
    '      column( \n',
    '        3, actionButton("{prefix}a1togL", "Toggle to subset cells"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}a1togL % 2 == 1", \n',
    '          selectInput("{prefix}a1sub1", "Cell information to subset:", \n',
    '                      choices = {prefix}conf[grp == TRUE]$UI, \n',
    '                      selected = {prefix}def$grp1), \n',
    '          uiOutput("{prefix}a1sub1.ui"), \n',
    '          actionButton("{prefix}a1sub1all", "Select all groups", class = "btn btn-primary"), \n',
    '          actionButton("{prefix}a1sub1non", "Deselect all groups", class = "btn btn-primary") \n',
    '        ) \n',
    '      ), # End of column (6 space) \n',
    '      column( \n',
    '        6, actionButton("{prefix}a1tog0", "Toggle graphics controls"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}a1tog0 % 2 == 1", \n',
    '          fluidRow( \n',
    '            column( \n',
    '              6, sliderInput("{prefix}a1siz", "Point size:", \n',
    '                             min = 0, max = 4, value = {ptsiz}, step = 0.25), \n',
    '              radioButtons("{prefix}a1psz", "Plot size:", \n',
    '                           choices = c("Small", "Medium", "Large"), \n',
    '                           selected = "Medium", inline = TRUE), \n',
    '              radioButtons("{prefix}a1fsz", "Font size:", \n',
    '                           choices = c("Small", "Medium", "Large"), \n',
    '                           selected = "Medium", inline = TRUE) \n',
    '            ), \n',
    '            column( \n',
    '              6, radioButtons("{prefix}a1asp", "Aspect ratio:", \n',
    '                              choices = c("Square", "Fixed", "Free"), \n',
    '                              selected = "Square", inline = TRUE), \n',
    '              checkboxInput("{prefix}a1txt", "Show axis text", value = FALSE), \n',
    '              colourInput("{prefix}a1bg", "Plot background colour:", value = "white") \n',
    '            ) \n',
    '          ) \n',
    '        ) \n',
    '      )  # End of column (6 space) \n',
    '    ),   # End of fluidRow (4 space) \n',
    '    fluidRow( \n',
    '      column( \n',
    '        3, style="border-right: 2px solid black", h4("Information to plot"),\n',
    '        selectInput("{prefix}a1ass1", "Data type to colour plot:",\n',
    '                    choices = c("Cell Information", paste0("Assay: ", {prefix}def$assay)), \n',
    '                    selected = "Cell Information"), \n',
  '        selectInput("{prefix}a1inp1", "Cell Info / Feature Name:", \n',
  '                    choices = {prefix}conf$UI, \n',
  '                    selected = {prefix}conf$UI[1]) %>% \n',
    '          helper(type = "inline", size = "m", fade = TRUE,\n',
    '                 title = "Cell Info / Gene to colour cells by",\n',
    '                 content = c("Select cell info / feature to colour cells",\n',
    '                             "- Categorical covariates have a fixed colour palette",\n',
    '                             paste0("- Continuous covariates / gene expression are coloured ",\n',
    '                                    "in a Blue-Yellow-Red colour scheme, which can be ",\n',
    '                                    "changed in the plot controls"))),\n',
    '        strong("Draw box on plot below to zoom: "), \n',
    '        plotOutput("{prefix}a1oup1.br", height = "400px",\n',
    '                   brush = brushOpts(id = "{prefix}a1inp1.br", resetOnNew = TRUE)), \n',
    '        actionButton("{prefix}a1tog1", "Toggle plot controls"), \n',
    '        conditionalPanel(\n',
    '          condition = "input.{prefix}a1tog1 % 2 == 1",\n',
    '          radioButtons("{prefix}a1col1", "Colour (Continuous data):",\n',
    '                       choices = c("White-Red","Blue-Yellow-Red","Yellow-Green-Purple"),\n',
    '                       selected = "Blue-Yellow-Red"),\n',
    '          numericInput("{prefix}a1min1", "Min cutoff (q##):",\n',
    '                       min = 0, max = 50, value = 0, step = 1),\n',
    '          numericInput("{prefix}a1max1", "Max cutoff (q##):",\n',
    '                       min = 50, max = 100, value = 100, step = 1),\n',
    '          radioButtons("{prefix}a1ord1", "Plot order:",\n',
    '                       choices = c("Max-1st", "Min-1st", "Original", "Random"),\n',
    '                       selected = "Original", inline = TRUE),\n',
    '          checkboxInput("{prefix}a1lab1", "Show cell info labels", value = TRUE)\n',
    '        )\n',
    '      ), # End of column (6 space) \n',
    '      column( \n',
    '        6, style="border-right: 2px solid black", \n',
    '        fluidRow(column(12, uiOutput("{prefix}a1oup1.ui"))),\n',
    '        fluidRow(column(12, uiOutput("{prefix}a1oup3.ui"))),\n',
    '        fluidRow(column(3, downloadButton("{prefix}a1oup1.dl", "Download Plot")),\n',
    '                 column(3, radioButtons("{prefix}a1oup1.f", "Plot format:", \n',
    '                                        choices = c("png", "pdf"), selected = "png", inline = TRUE)),\n',
    '                 column(3, numericInput("{prefix}a1oup1.h", "Plot height:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5)),\n',
    '                 column(3, numericInput("{prefix}a1oup1.w", "Plot width:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5))),\n',
    '        fluidRow(column(3, downloadButton("{prefix}a1oup3.dl", "Download Legend")),\n',
    '                 column(3, radioButtons("{prefix}a1oup3.f", "Legend format:", \n',
    '                                        choices = c("png", "pdf"), selected = "png", inline = TRUE)),\n',
    '                 column(3, numericInput("{prefix}a1oup3.h", "Legend height:",\n',
    '                                        min = 0.2, max = 10, value = 2, step = 0.1)),\n',
    '                 column(3, numericInput("{prefix}a1oup3.w", "Legend width:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5))),\n',
    '        br() \n',
    '      ), # End of column (6 space) \n',
    '      column(\n',
    '        3, h4("Cell numbers"), \n',
    '        numericInput("{prefix}a1splt", "Split continuous cell info into nBins:",\n',
    '                     min = 2, max = 10, value = 4, step = 1),\n',
    '        dataTableOutput("{prefix}a1.dt") \n',
    '      )  # End of column (6 space) \n',
    '    )    # End of fluidRow (4 space) \n',
    '  )      # End of tab (2 space) \n',
    '  , \n',
    '  \n'
  )
}


#' Write code for ui.R
#'
#' @rdname wrUImainS3
#' @export wrUImainS3
#'
wrUImainS3 <- function(prefix, ptsiz) {
  glue::glue(
    '  ### Tab1.s3: Split-out dimred\n',
    '  tabPanel(\n',
    '    HTML("Split<span class=\'nav-sub\'>DimRed</span>"),\n',
    '    h4("Faceted plots for dimensional reduction"),\n',
    '    "Split on metadata",\n',
    '    br(), br(),\n',
    '    # Assay selection\n',
    '    fluidRow(\n',
    '      column(3, selectInput("{prefix}s3ass1", "Assay:",\n',
    '        choices = {prefix}def$assay,\n',
    '        selected = {prefix}def$assay[1]\n',
    '      ))\n',
    '    ),\n',
    '    # Reduction selection\n',
    '    fluidRow(\n',
    '      column(\n',
    '        3, fluidRow(\n',
    '          column(\n',
    '            12, selectInput("{prefix}s3dr", "Reduction:",\n',
    '              choices = {prefix}def$dimrd,\n',
    '              selected = {prefix}def$dimrd[1]\n',
    '            )\n',
    '          )\n',
    '        )\n',
    '      )\n',
    '    ), # End of column (6 space)\n',
    '    fluidRow(\n',
    '      column(\n',
    '        3,\n',
    '        style = "border-right: 2px solid black",\n',
    '        # Split by selection\n',
    '        selectInput("{prefix}s3inp3", "Split by",\n',
    '          choices = {prefix}conf[grp == TRUE]$UI,\n',
    '          selected = {prefix}def$grp1\n',
    '        ) %>% helper(\n',
    '            type = "inline", size = "m", fade = TRUE,\n',
    '            title = "Cell information to group cells by",\n',
    '            content = c("Select categorical cell information to split single cells by this categorical covariate")\n',
    '          ),\n',
    '        selectizeInput(\n',
    '          "{prefix}s3feat1",\n',
    '          "Gene/feature:",\n',
    '          choices = {prefix}def$genes[[{prefix}def$assay[1]]],\n',
    '          selected = {prefix}def$genes[[{prefix}def$assay[1]]][[1]],\n',
    '          options = list(\n',
    '            maxOptions = 7,\n',
    '            create = FALSE,\n',
    '            persist = TRUE\n',
    '          )\n',
    '        ),\n',
    '        br(), br(),\n',
    '        actionButton("{prefix}s3togL", "Toggle to subset cells"),\n',
    '        conditionalPanel(\n',
    '          condition = "input.{prefix}s3togL % 2 == 1",\n',
    '          selectInput("{prefix}s3sub1", "Cell information to subset:",\n',
    '            choices = {prefix}conf[grp == TRUE]$UI,\n',
    '            selected = {prefix}def$grp1\n',
    '          ),\n',
    '          uiOutput("{prefix}s3sub1.ui"),\n',
    '          actionButton("{prefix}s3sub1all", "Select all groups", class = "btn btn-primary"),\n',
    '          actionButton("{prefix}s3sub1non", "Deselect all groups", class = "btn btn-primary")\n',
    '        ),\n',
    '        br(), br(),\n',
    '        actionButton("{prefix}s3tog2", "Toggle graphics controls"),\n',
    '        conditionalPanel(\n',
    '          condition = "input.{prefix}s3tog2 % 2 == 1",\n',
    '          sliderInput("{prefix}s3siz", "Point size:",\n',
    '            min = 1, max = 4, value = 1.25, step = 0.25\n',
    '          ),\n',
    '          radioButtons("{prefix}s3psz", "Plot size:",\n',
    '            choices = c("Small", "Medium", "Large"),\n',
    '            selected = "Large", inline = TRUE\n',
    '          ),\n',
    '          radioButtons("{prefix}s3fsz", "Font size:",\n',
    '            choices = c("Small", "Medium", "Large"),\n',
    '            selected = "Large", inline = TRUE\n',
    '          ),\n',
    '          radioButtons("{prefix}s3asp", "Aspect ratio:",\n',
    '            choices = c("Square", "Fixed", "Free"),\n',
    '            selected = "Free", inline = TRUE\n',
    '          ),\n',
    '          checkboxInput("{prefix}s3txt", "Show axis text", value = FALSE),\n',
    '          colourInput("{prefix}s3bg", "Plot background colour:", value = "white")\n',
    '        ),\n',
    '        br(), br(),\n',
    '        actionButton("{prefix}s3tog3", "Toggle plot controls"),\n',
    '        conditionalPanel(\n',
    '          condition = "input.{prefix}s3tog3 % 2 == 1",\n',
    '          radioButtons("{prefix}s3col1", "Colour (Continuous data):",\n',
    '            choices = c("White-Red", "Blue-Yellow-Red", "Yellow-Green-Purple"),\n',
    '            selected = "Blue-Yellow-Red"\n',
    '          ),\n',
    '          radioButtons("{prefix}s3ord1", "Plot order:",\n',
    '            choices = c("Max-1st", "Min-1st", "Original", "Random"),\n',
    '            selected = "Original", inline = TRUE\n',
    '          )\n',
    '        ),\n',
    '      ), # End of column (6 space)\n',
    '      column(\n',
    '        9, uiOutput("{prefix}s3oup1.ui"),\n',
    '        downloadButton("{prefix}s3oup1.pdf", "Download PDF"),\n',
    '        downloadButton("{prefix}s3oup1.png", "Download PNG"), br(),\n',
    '        div(\n',
    '          style = "display:inline-block",\n',
    '          numericInput("{prefix}s3oup1.h", "PDF / PNG height:",\n',
    '            width = "138px",\n',
    '            min = 4, max = 20, value = 8, step = 0.5\n',
    '          )\n',
    '        ),\n',
    '        div(\n',
    '          style = "display:inline-block",\n',
    '          numericInput("{prefix}s3oup1.w", "PDF / PNG width:",\n',
    '            width = "138px",\n',
    '            min = 4, max = 20, value = 10, step = 0.5\n',
    '          )\n',
    '        )\n',
    '      ) # End of column (6 space)\n',
    '    ) # End of fluidRow (4 space)\n',
    '  ), # End of tab (2 space)\n',
    '  \n'
  )
}


#' Write code for ui.R
#'
#' @rdname wrUImainD1
#' @export wrUImainD1
#'
wrUImainD1 <- function(prefix, ptsiz) {
  glue::glue(
    '### Tab1.d1: DEG dimred plot\n',
    'tabPanel(\n',
    '  HTML("DEG dimred plot"),\n',
    '  h4("Inspect cluster markers on dimensional reduction plots"),\n',
    '  "Select clusters from the drop down menu to visualise their expression ",\n',
    '  "patterns on low-dimensional representions.",\n',
    '  br(), br(),\n',
    '  fluidRow(\n',
    '    column(\n',
    '      3,\n',
    '      selectInput("{prefix}d1ass1", "Assay:",\n',
    '        choices = {prefix}def$assay,\n',
    '        selected = {prefix}def$assay[1]\n',
    '      ),\n',
    '      selectInput("{prefix}d1dr", "Reduction:",\n',
    '        choices = {prefix}def$dimrd,\n',
    '        selected = {prefix}def$dimrd[1]\n',
    '      ),\n',
    '      selectInput("{prefix}d1deglab", "Cluster labels:",\n',
    '        choices = {prefix}def$DEG,\n',
    '        selected = {prefix}def$DEG[[1]]\n',
    '      ),\n',
    '      selectInput("{prefix}d1degcluster", "Select cluster:",\n',
    '        choices = levels(as.data.frame(sc1meta)[,sc1def$meta2]),\n',
    '        selected = levels(as.data.frame(sc1meta)[,sc1def$meta2])[[1]]\n',
    '      ),\n',
    '    ), # End of column (6 space)\n',
    '    column(\n',
    '      4, actionButton("{prefix}d1tog0", "Toggle graphics controls"),\n',
    '      conditionalPanel(\n',
    '        condition = "input.{prefix}d1tog0 % 2 == 1",\n',
    '        fluidRow(\n',
    '          column(\n',
    '            6, sliderInput("{prefix}d1siz", "Point size:",\n',
    '              min = 0, max = 4, value = 1.25, step = 0.25\n',
    '            ),\n',
    '            radioButtons("{prefix}d1psz", "Plot size:",\n',
    '              choices = c("Small", "Medium", "Large"),\n',
    '              selected = "Medium", inline = TRUE\n',
    '            ),\n',
    '            radioButtons("{prefix}d1fsz", "Font size:",\n',
    '              choices = c("Small", "Medium", "Large"),\n',
    '              selected = "Medium", inline = TRUE\n',
    '            )\n',
    '          ),\n',
    '          column(\n',
    '            6, radioButtons("{prefix}d1asp", "Aspect ratio:",\n',
    '              choices = c("Square", "Fixed", "Free"),\n',
    '              selected = "Square", inline = TRUE\n',
    '            ),\n',
    '          )\n',
    '        )\n',
    '      )\n',
    '    ),\n',
    '    column(\n',
    '      5,\n',
    '      style = "border-right: 2px solid black",\n',
    '      h4(""),\n',
    '      actionButton("{prefix}d1tog1", "Toggle table controls"),\n',
    '      conditionalPanel(\n',
    '        condition = "input.{prefix}d1tog1 % 2 == 1",\n',
    '        fluidRow(\n',
    '          column(\n',
    '            6,\n',
    '            sliderInput(\n',
    '              "{prefix}d1pval",\n',
    '              "Maximum p-value:",\n',
    '              min = 0.001,\n',
    '              max = 1,\n',
    '              value = 0.05,\n',
    '              step = 0.001\n',
    '            ),\n',
    '            sliderInput(\n',
    '              "{prefix}d1pvaladj",\n',
    '              HTML("Maximum <u>adjusted</u> p-value:"),\n',
    '              min = 0.001,\n',
    '              max = 1,\n',
    '              value = 0.05,\n',
    '              step = 0.001\n',
    '            ),\n',
    '            sliderInput(\n',
    '              "{prefix}d1log2fc",\n',
    '              HTML("Minimum <u>absolute</u> log2 fold change:"),\n',
    '              min = 0,\n',
    '              max = 5,\n',
    '              value = 1.0,\n',
    '              step = 0.05\n',
    '            ),\n',
    '          ),\n',
    '          column(\n',
    '            6,\n',
    '            checkboxInput(\n',
    '              "{prefix}d1diff",\n',
    '              HTML("Show <b><u> absolute diff(pc.1, pc.2)</u></b>"),\n',
    '              value = FALSE\n',
    '            ),\n',
    '            conditionalPanel(\n',
    '              condition = "input.{prefix}d1diff == true",\n',
    '              sliderInput(\n',
    '                "{prefix}d1pctdiffmin",\n',
    '                HTML("Minimum pct.diff:"),\n',
    '                min = 0,\n',
    '                max = 1,\n',
    '                value = 0,\n',
    '                step = 0.05\n',
    '              ),\n',
    '              sliderInput(\n',
    '                "{prefix}d1pctdiffmax",\n',
    '                HTML("Maximum pct.diff:"),\n',
    '                min = 0,\n',
    '                max = 1,\n',
    '                value = 1.0,\n',
    '                step = 0.05\n',
    '              )\n',
    '            )\n',
    '          )\n',
    '        )\n',
    '      )\n',
    '    ), # End of column (6 space)\n',
    '  ), # End of fluidRow (4 space)\n',
    '  fluidRow(\n',
    '    column(\n',
    '      7,\n',
    '      style = "border-right: 2px solid black",\n',
    '      uiOutput("{prefix}d1oup1.ui"),\n',
    '      fluidRow(\n',
    '        column(3, downloadButton("{prefix}d1oup1.dl", "Download Plot")), \n',
    '        column(3, radioButtons("{prefix}d1oup1.f", "Plot format:",\n',
    '          choices = c("png", "pdf"), selected = "png", inline = TRUE\n',
    '        )), \n',
    '        column(3, numericInput("{prefix}d1oup1.h", "Plot height:",\n',
    '          min = 4, max = 20, value = 8, step = 0.5\n',
    '        )),\n',
    '        column(3, numericInput("{prefix}d1oup1.w", "Plot width:",\n',
    '          min = 4, max = 20, value = 8, step = 0.5\n',
    '        ))\n',
    '      ),\n',
    '      br()\n',
    '    ), # End of column (6 space)\n',
    '    column(\n',
    '      5,\n',
    '      br(), h4("DEG markers numbers"),\n',
    '      dataTableOutput("{prefix}d1.dt")\n',
    '    ) # End of column (6 space)\n',
    '  ) # End of fluidRow (4 space)\n',
    ') # End of tab (2 space)\n',
    ',\n'
  )
}


#' Write code for ui.R
#'
#' @rdname wrUImainA2
#' @export wrUImainA2
#'
wrUImainA2 <- function(prefix, ptsiz) {
  glue::glue(
    '  ### Tab1.a2: CellInfo vs AssayExpr on dimRed \n',
    '  tabPanel( \n',
    '    HTML("Side-by-side<span class=\'nav-sub\'>DimRed</span>"), \n',
    '    h4("Cell information vs assay expression on reduced dimensions"), \n',
    '    "In this tab, users can visualise both cell information and gene ",  \n',
    '    "expression side-by-side on low-dimensional representions.", \n',
    '    br(),br(), \n',
    '    HTML(paste0(\n',
    '      "<div style=\'font-size:13px; margin-bottom:8px;\'>",\n',
    '      "<b>Selector colour key:</b>&nbsp;&nbsp;",\n',
    '      "<span style=\'color:#1B7837; font-weight:bold;\'>&#9632; Categorical</span>&nbsp;&nbsp;",\n',
    '      "<span style=\'color:#2166AC; font-weight:bold;\'>&#9632; Continuous</span>&nbsp;&nbsp;",\n',
    '      "<span style=\'color:#E66101; font-weight:bold;\'>&#9632; Undetermined</span>",\n',
    '      "</div>")),\n',
    '    fluidRow( \n',
    '      column( \n',
    '        3, fluidRow(\n',
    '          column(\n',
    '            12, selectInput("{prefix}a2dr", "Reduction:", choices = {prefix}def$dimrd, \n',
    '                            selected = {prefix}def$dimrd[1]))\n',
    '        ) \n',
    '      ), # End of column (6 space) \n',
    '      column( \n',
    '        3, actionButton("{prefix}a2togL", "Toggle to subset cells"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}a2togL % 2 == 1", \n',
    '          selectInput("{prefix}a2sub1", "Cell information to subset:", \n',
    '                      choices = {prefix}conf[grp == TRUE]$UI, \n',
    '                      selected = {prefix}def$grp1), \n',
    '          uiOutput("{prefix}a2sub1.ui"), \n',
    '          actionButton("{prefix}a2sub1all", "Select all groups", class = "btn btn-primary"), \n',
    '          actionButton("{prefix}a2sub1non", "Deselect all groups", class = "btn btn-primary") \n',
    '        ) \n',
    '      ), # End of column (6 space) \n',
    '      column( \n',
    '        6, actionButton("{prefix}a2tog0", "Toggle graphics controls"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}a2tog0 % 2 == 1", \n',
    '          fluidRow( \n',
    '            column( \n',
    '              6, sliderInput("{prefix}a2siz", "Point size:", \n',
    '                             min = 0, max = 4, value = {ptsiz}, step = 0.25), \n',
    '              radioButtons("{prefix}a2psz", "Plot size:", \n',
    '                           choices = c("Small", "Medium", "Large"), \n',
    '                           selected = "Medium", inline = TRUE), \n',
    '              radioButtons("{prefix}a2fsz", "Font size:", \n',
    '                           choices = c("Small", "Medium", "Large"), \n',
    '                           selected = "Medium", inline = TRUE) \n',
    '            ), \n',
    '            column( \n',
    '              6, radioButtons("{prefix}a2asp", "Aspect ratio:", \n',
    '                              choices = c("Square", "Fixed", "Free"), \n',
    '                              selected = "Square", inline = TRUE), \n',
    '              checkboxInput("{prefix}a2txt", "Show axis text", value = FALSE), \n',
    '              colourInput("{prefix}a2bg", "Plot background colour:", value = "white") \n',
    '            ) \n',
    '          ) \n',
    '        ) \n',
    '      )  # End of column (6 space) \n',
    '    ),   # End of fluidRow (4 space) \n',
    '    fluidRow( \n',
    '      column(\n',
    '        6, style="border-right: 2px solid black",\n',
    '        fluidRow(\n',
    '          column(\n',
    '            6, selectInput("{prefix}a2ass1", "Data type to colour plot:",\n',
    '                           choices = c("Cell Information", paste0("Assay: ", {prefix}def$assay)), \n',
    '                           selected = "Cell Information"), \n',
    '            div(class = "centerSelect",\n',
    '            selectInput("{prefix}a2inp1", "Cell Info / Feature Name:", choices = NULL) %>% \n',
    '              helper(type = "inline", size = "m", fade = TRUE,\n',
    '                     title = "Cell Info / Gene to colour cells by",\n',
    '                     content = c("Select cell info / feature to colour cells",\n',
    '                                 "- Categorical covariates have a fixed colour palette",\n',
    '                                 paste0("- Continuous covariates / gene expression are coloured ",\n',
    '                                        "in a Blue-Yellow-Red colour scheme, which can be ",\n',
    '                                        "changed in the plot controls"))))\n',
    '          ),\n',
    '          column(\n',
    '            6, actionButton("{prefix}a2tog1", "Toggle plot controls"),\n',
    '            conditionalPanel(\n',
    '              condition = "input.{prefix}a2tog1 % 2 == 1",\n',
    '              radioButtons("{prefix}a2col1", "Colour (Continuous data):",\n',
    '                           choices = c("White-Red","Blue-Yellow-Red","Yellow-Green-Purple"),\n',
    '                           selected = "Blue-Yellow-Red"),\n',
    '              numericInput("{prefix}a2min1", "Min cutoff (q##):",\n',
    '                           min = 0, max = 50, value = 0, step = 1),\n',
    '              numericInput("{prefix}a2max1", "Max cutoff (q##):",\n',
    '                           min = 50, max = 100, value = 100, step = 1),\n',
    '              radioButtons("{prefix}a2ord1", "Plot order:",\n',
    '                           choices = c("Max-1st", "Min-1st", "Original", "Random"),\n',
    '                           selected = "Original", inline = TRUE),\n',
    '              checkboxInput("{prefix}a2lab1", "Show cell info labels", value = TRUE)\n',
    '            )\n',
    '          )\n',
    '        ),\n',
    '        fluidRow(column(12, uiOutput("{prefix}a2oup1.ui"))),\n',
    '        fluidRow(column(12, uiOutput("{prefix}a2oup3.ui"))),\n',
    '        fluidRow(column(3, downloadButton("{prefix}a2oup1.dl", "Download Plot")),\n',
    '                 column(3, radioButtons("{prefix}a2oup1.f", "Plot format:", \n',
    '                                        choices = c("png", "pdf"), selected = "png", inline = TRUE)),\n',
    '                 column(3, numericInput("{prefix}a2oup1.h", "Plot height:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5)),\n',
    '                 column(3, numericInput("{prefix}a2oup1.w", "Plot width:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5))),\n',
    '        fluidRow(column(3, downloadButton("{prefix}a2oup3.dl", "Download Legend")),\n',
    '                column(3, radioButtons("{prefix}a2oup3.f", "Legend format:", \n',
    '                                       choices = c("png", "pdf"), selected = "png", inline = TRUE)),\n',
    '                 column(3, numericInput("{prefix}a2oup3.h", "Legend height:",\n',
    '                                        min = 0.2, max = 10, value = 2, step = 0.1)),\n',
    '                 column(3, numericInput("{prefix}a2oup3.w", "Legend width:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5))),\n',
    '        br() \n',
    '      ), # End of column (6 space)\n',
    '      column( \n',
    '        6,\n',
    '        fluidRow( \n',
    '          column(\n',
    '            6, selectInput("{prefix}a2ass2", "Data type to colour plot:",\n',
    '                           choices = c("Cell Information", paste0("Assay: ", {prefix}def$assay)), \n',
    '                           selected = paste0("Assay: ", {prefix}def$assay[1])), \n',
    '            div(class = "centerSelect",\n',
    '            selectInput("{prefix}a2inp2", "Cell Info / Feature Name:", choices = NULL) %>% \n',
    '              helper(type = "inline", size = "m", fade = TRUE,\n',
    '                     title = "Cell Info / Gene to colour cells by",\n',
    '                     content = c("Select cell info / feature to colour cells",\n',
    '                                 "- Categorical covariates have a fixed colour palette",\n',
    '                                 paste0("- Continuous covariates / gene expression are coloured ",\n',
    '                                        "in a Blue-Yellow-Red colour scheme, which can be ",\n',
    '                                        "changed in the plot controls"))))\n',
    '          ),\n',
    '          column(\n',
    '            6, actionButton("{prefix}a2tog2", "Toggle plot controls"),\n',
    '            conditionalPanel(\n',
    '              condition = "input.{prefix}a2tog2 % 2 == 1",\n',
    '              radioButtons("{prefix}a2col2", "Colour (Continuous data):",\n',
    '                           choices = c("White-Red","Blue-Yellow-Red","Yellow-Green-Purple"),\n',
    '                           selected = "White-Red"),\n',
    '              numericInput("{prefix}a2min2", "Min cutoff (q##):",\n',
    '                           min = 0, max = 50, value = 0, step = 1),\n',
    '              numericInput("{prefix}a2max2", "Max cutoff (q##):",\n',
    '                           min = 50, max = 100, value = 100, step = 1),\n',
    '              radioButtons("{prefix}a2ord2", "Plot order:",\n',
    '                           choices = c("Max-1st", "Min-1st", "Original", "Random"),\n',
    '                           selected = "Max-1st", inline = TRUE),\n',
    '              checkboxInput("{prefix}a2lab2", "Show cell info labels", value = TRUE)\n',
    '            )\n',
    '          )\n',
    '        ) , \n',
    '        fluidRow(column(12, uiOutput("{prefix}a2oup2.ui"))),\n',
    '        fluidRow(column(12, uiOutput("{prefix}a2oup4.ui"))),\n',
    '        fluidRow(column(3, downloadButton("{prefix}a2oup2.dl", "Download Plot")),\n',
    '                 column(3, radioButtons("{prefix}a2oup2.f", "Plot format:", \n',
    '                                        choices = c("png", "pdf"), selected = "png", inline = TRUE)),\n',
    '                 column(3, numericInput("{prefix}a2oup2.h", "Plot height:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5)),\n',
    '                 column(3, numericInput("{prefix}a2oup2.w", "Plot width:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5))),\n',
    '        fluidRow(column(3, downloadButton("{prefix}a2oup4.dl", "Download Legend")),\n',
    '                 column(3, radioButtons("{prefix}a2oup4.f", "Legend format:", \n',
    '                                        choices = c("png", "pdf"), selected = "png", inline = TRUE)),\n',
    '                 column(3, numericInput("{prefix}a2oup4.h", "Legend height:",\n',
    '                                        min = 0.2, max = 10, value = 2, step = 0.1)),\n',
    '                 column(3, numericInput("{prefix}a2oup4.w", "Legend width:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5))),\n',
    '        br()\n',
    '      )  # End of column (6 space) \n',
    '    ),   # End of fluidRow (4 space) \n',
    '    h4("Relationship between left-side and right-side cell info / feature"),\n',
    '    fluidRow(\n',
    '      column(\n',
    '        6, h4("Comparative plot"), \n',
    '        style="border-right: 2px solid black", \n',
    '        uiOutput("{prefix}a2oup5.ui"), \n',
    '        fluidRow(column(3, downloadButton("{prefix}a2oup5.dl", "Download Plot")),\n',
    '                 column(3, radioButtons("{prefix}a2oup5.f", "Plot format:", \n',
    '                                        choices = c("png", "pdf"), selected = "png", inline = TRUE)),\n',
    '                 column(3, numericInput("{prefix}a2oup5.h", "Plot height:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5)),\n',
    '                 column(3, numericInput("{prefix}a2oup5.w", "Plot width:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5)))\n',
    '      ),\n',
    '      column(\n',
    '        6, h4("Cell numbers / statistics"), \n',
    '        dataTableOutput("{prefix}a2.dt") \n',
    '      )  # End of column (6 space) \n',
    '    )    # End of fluidRow (4 space) \n',
    '  )      # End of tab (2 space) \n',
    '  , \n',
    '  \n'
  )
}

#' Write code for ui.R
#'
#' @rdname wrUImainA3
#' @export wrUImainA3
#'
wrUImainA3 <- function(prefix, ptsiz) {
  glue::glue(
    '  ### Tab1.a3: Gene coexpression plot \n',
    '  tabPanel( \n',
    '    HTML("Gene CoExp"), \n',
    '    h4("Coexpression of two genes on reduced dimensions"), \n',
    '    "In this tab, users can visualise the coexpression of two genes ", \n',
    '    "on low-dimensional representions.", \n',
    '    br(),br(), \n',
    '    fluidRow( \n',
    '      column( \n',
    '        3, fluidRow( \n',
    '          column( \n',
    '            12, selectInput("{prefix}a3dr", "Reduction:", choices = {prefix}def$dimrd, \n',
    '                            selected = {prefix}def$dimrd[1])) \n',
    '        ) \n',
    '      ), # End of column (6 space) \n',
    '      column( \n',
    '        3, actionButton("{prefix}a3togL", "Toggle to subset cells"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}a3togL % 2 == 1", \n',
    '          selectInput("{prefix}a3sub1", "Cell information to subset:", \n',
    '                      choices = {prefix}conf[grp == TRUE]$UI, \n',
    '                      selected = {prefix}def$grp1), \n',
    '          uiOutput("{prefix}a3sub1.ui"), \n',
    '          actionButton("{prefix}a3sub1all", "Select all groups", class = "btn btn-primary"), \n',
    '          actionButton("{prefix}a3sub1non", "Deselect all groups", class = "btn btn-primary") \n',
    '        ) \n',
    '      ), # End of column (6 space) \n',
    '      column( \n',
    '        6, actionButton("{prefix}a3tog0", "Toggle graphics controls"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}a3tog0 % 2 == 1", \n',
    '          fluidRow( \n',
    '            column( \n',
    '              6, sliderInput("{prefix}a3siz", "Point size:", \n',
    '                             min = 0, max = 4, value = {ptsiz}, step = 0.25), \n',
    '              radioButtons("{prefix}a3psz", "Plot size:", \n',
    '                           choices = c("Small", "Medium", "Large"), \n',
    '                           selected = "Medium", inline = TRUE), \n',
    '              radioButtons("{prefix}a3fsz", "Font size:", \n',
    '                           choices = c("Small", "Medium", "Large"), \n',
    '                           selected = "Medium", inline = TRUE) \n',
    '            ), \n',
    '            column( \n',
    '              6, radioButtons("{prefix}a3asp", "Aspect ratio:", \n',
    '                              choices = c("Square", "Fixed", "Free"), \n',
    '                              selected = "Square", inline = TRUE), \n',
    '              checkboxInput("{prefix}a3txt", "Show axis text", value = FALSE), \n',
    '              colourInput("{prefix}a3bg", "Plot background colour:", value = "white") \n',
    '            ) \n',
    '          ) \n',
    '        ) \n',
    '      )  # End of column (6 space) \n',
    '    ),   # End of fluidRow (4 space) \n',
    '    fluidRow( \n',
    '      column( \n',
    '        3, style="border-right: 2px solid black", h4("Assay Expression"), \n',
    '        selectInput("{prefix}a3inp1", "Feature 1:", choices=NULL) %>%  \n',
    '          helper(type = "inline", size = "m", fade = TRUE, \n',
    '                title = "Feature expression to colour cells by", \n',
    '                content = c("Select gene to colour cells by gene expression", \n',
    '                           paste0("- Feature expression are coloured in a ", \n',
    '                                  "White-Red colour scheme which can be ", \n',
    '                                  "changed in the plot controls"))), \n',
    '        selectInput("{prefix}a3inp2", "Feature 2:", choices=NULL) %>% \n',
    '          helper(type = "inline", size = "m", fade = TRUE, \n',
    '                 title = "Feature expression to colour cells by", \n',
    '                 content = c("Select gene to colour cells by gene expression", \n',
    '                             paste0("- Feature expression are coloured in a ", \n',
    '                                    "White-Blue colour scheme which can be ", \n',
    '                                    "changed in the plot controls"))), \n',
    '        selectInput("{prefix}a3ass1", "Assay:", \n',
    '                    choices = {prefix}def$assay, \n',
    '                    selected = {prefix}def$assay[1]), \n',
    '        actionButton("{prefix}a3tog1", "Toggle plot controls"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}a3tog1 % 2 == 1", \n',
    '          radioButtons("{prefix}a3col1", "Colour:", \n',
    '                       choices = c("Red (Gene1); Blue (Gene2)", \n',
    '                                   "Orange (Gene1); Blue (Gene2)", \n',
    '                                   "Red (Gene1); Green (Gene2)", \n',
    '                                   "Green (Gene1); Blue (Gene2)"), \n',
    '                       selected = "Red (Gene1); Blue (Gene2)"), \n',
    '          numericInput("{prefix}a3min1", "Min cutoff gene 1 (q##):", \n',
    '                       min = 0, max = 50, value = 0, step = 1), \n',
    '          numericInput("{prefix}a3max1", "Max cutoff gene 1 (q##):", \n',
    '                       min = 50, max = 100, value = 100, step = 1), \n',
    '          numericInput("{prefix}a3min2", "Min cutoff gene 2 (q##):", \n',
    '                       min = 0, max = 50, value = 0, step = 1), \n',
    '          numericInput("{prefix}a3max2", "Max cutoff gene 2 (q##):", \n',
    '                       min = 50, max = 100, value = 100, step = 1), \n',
    '          radioButtons("{prefix}a3ord1", "Plot order:", \n',
    '                       choices = c("Max-1st", "Min-1st", "Original", "Random"), \n',
    '                       selected = "Max-1st", inline = TRUE) \n',
    '        ) \n',
    '      ), # End of column (6 space) \n',
    '      column( \n',
    '        6, style="border-right: 2px solid black", \n',
    '        uiOutput("{prefix}a3oup1.ui"), \n',
    '        fluidRow(column(3, downloadButton("{prefix}a3oup1.dl", "Download Plot")), \n',
    '                 column(3, radioButtons("{prefix}a3oup1.f", "Plot format:", \n',
    '                                        choices = c("png", "pdf"), selected = "png", inline = TRUE)), \n',
    '                 column(3, numericInput("{prefix}a3oup1.h", "Plot height:", \n',
    '                                        min = 4, max = 20, value = 8, step = 0.5)), \n',
    '                 column(3, numericInput("{prefix}a3oup1.w", "Plot width:", \n',
    '                                        min = 4, max = 20, value = 8, step = 0.5))), \n',
    '        br() \n',
    '      ), # End of column (6 space) \n',
    '      column( \n',
    '        3, uiOutput("{prefix}a3oup2.ui"), \n',
    '        fluidRow(column(6, downloadButton("{prefix}a3oup2.dl", "Download Legend")), \n',
    '                 column(6, radioButtons("{prefix}a3oup2.f", "Legend format:", \n',
    '                        choices = c("png", "pdf"), selected = "png", inline = TRUE))), \n',
    '        br(), h4("Cell numbers"), \n',
    '        dataTableOutput("{prefix}a3.dt") \n',
    '      )  # End of column (6 space) \n',
    '    )    # End of fluidRow (4 space) \n',
    '  )      # End of tab (2 space) \n',
    '  , \n',
    '  \n'
  )
}

#' Write code for ui.R
#'
#' @rdname wrUImainB1
#' @export wrUImainB1
#'
wrUImainB1 <- function(prefix, ptsiz) {
  glue::glue(
    '  ### Tab1.b1: violinplot / boxplot \n',
    '  tabPanel( \n',
    '    HTML("Vln/Box plots"),  \n',
    '    h4("Cell information / assay expression violin plot / box plot"), \n',
    '    "In this tab, users can visualise the assay expression or continuous cell information ",  \n',
    '    "(e.g. Number of UMIs / module score) across groups of cells (e.g. libary / clusters).", \n',
    '    br(),br(), \n',
    '    fluidRow( \n',
    '      column( \n',
    '        3, style="border-right: 2px solid black", \n',
    '        selectInput("{prefix}b1inp1", "Cell information (X-axis):", \n',
    '                    choices = {prefix}conf[grp == TRUE]$UI, \n',
    '                    selected = {prefix}def$grp1) %>%  \n',
    '          helper(type = "inline", size = "m", fade = TRUE, \n',
    '                 title = "Cell information to group cells by",  \n',
    '                 content = c("Select categorical cell information to group cells by",  \n',
    '                             "- Single cells are grouped by this categorical covariate",  \n',
    '                             "- Plotted as the X-axis of the violin plot / box plot")),  \n',
    '        selectInput("{prefix}b1inp2", "Cell Info / Feature name (Y-axis):", choices=NULL) %>%  \n',
    '          helper(type = "inline", size = "m", fade = TRUE, \n',
    '                 title = "Cell Info / Gene to plot", \n',
    '                 content = c("Select cell info / feature to plot on Y-axis", \n',
    '                             "- Can be continuous cell information (e.g. nUMIs / scores)", \n',
    '                             "- Can also be feature expression")), \n',
    '        selectInput("{prefix}b1ass1", "Data type for Y-axis:", \n',
    '                    choices = c("Cell Information", paste0("Assay: ", {prefix}def$assay)), \n',
    '                    selected = "Cell Information"), \n',
    '        radioButtons("{prefix}b1typ", "Plot type:", \n',
    '                     choices = c("violin", "boxplot"), \n',
    '                     selected = "violin", inline = TRUE), \n',
    '        checkboxInput("{prefix}b1pts", "Show data points", value = FALSE), \n',
    '        actionButton("{prefix}b1togT", "Toggle to perform stats test(s)"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}b1togT % 2 == 1",  \n',
    '          radioButtons("{prefix}b1stg", "Perform global test:",  \n',
    '                       choices = c("none","kruskal.test","anova"),  \n',
    '                       selected = "none"), \n',
    '          radioButtons("{prefix}b1stp1", "Perform pairwise test:",  \n',
    '                       choices = c("none","wilcox.test","t.test"),  \n',
    '                       selected = "none"), \n',
    '          textAreaInput("{prefix}b1stp2", HTML("Pairwise comparisons to make <br />  \n',
    '                                               (Separate each pair by new line and <br />  \n',
    '                                               within each pair using , or ;):"),  \n',
    '                        height = "150px", value = "none") \n',
    '        ), br(), br(),  \n',
    '        actionButton("{prefix}b1togL", "Toggle to subset cells"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}b1togL % 2 == 1", \n',
    '          selectInput("{prefix}b1sub1", "Cell information to subset:", \n',
    '                      choices = {prefix}conf[grp == TRUE]$UI, \n',
    '                      selected = {prefix}def$grp1), \n',
    '          uiOutput("{prefix}b1sub1.ui"), \n',
    '          actionButton("{prefix}b1sub1all", "Select all groups", class = "btn btn-primary"), \n',
    '          actionButton("{prefix}b1sub1non", "Deselect all groups", class = "btn btn-primary") \n',
    '        ), br(), br(), \n',
    '        actionButton("{prefix}b1tog", "Toggle graphics controls"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}b1tog % 2 == 1", \n',
    '          sliderInput("{prefix}b1siz", "Data point size:",  \n',
    '                      min = 0, max = 4, value = {ptsiz}, step = 0.25),  \n',
    '          radioButtons("{prefix}b1psz", "Plot size:", \n',
    '                       choices = c("Small", "Medium", "Large"), \n',
    '                       selected = "Medium", inline = TRUE), \n',
    '          radioButtons("{prefix}b1fsz", "Font size:", \n',
    '                       choices = c("Small", "Medium", "Large"), \n',
    '                       selected = "Medium", inline = TRUE), \n',
    '          checkboxInput("{prefix}b1noi", "Add noise to assay expr", value = TRUE),\n',
    '          colourInput("{prefix}b1bg", "Plot background colour:", value = "white")) \n',
    '      ), # End of column (6 space) \n',
    '      column(9, uiOutput("{prefix}b1oup.ui"), \n',
    '      fluidRow(column(2, downloadButton("{prefix}b1oup.dl", "Download Plot")), \n',
    '               column(2, radioButtons("{prefix}b1oup.f", "Plot format:", \n',
    '                                      choices = c("png", "pdf"), selected = "png", inline = TRUE)), \n',
    '               column(2, numericInput("{prefix}b1oup.h", "Plot height:", \n',
    '                                      min = 4, max = 20, value = 8, step = 0.5)), \n',
    '               column(2, numericInput("{prefix}b1oup.w", "Plot width:", \n',
    '                                      min = 4, max = 20, value = 10, step = 0.5))), \n',
    '      br() \n',
    '      )  # End of column (6 space) \n',
    '    )    # End of fluidRow (4 space) \n',
    '  )      # End of tab (2 space) \n',
    '  , \n',
    '  \n'
  )
}

#' Write code for ui.R
#'
#' @rdname wrUImainB2
#' @export wrUImainB2
#'
wrUImainB2 <- function(prefix) {
  glue::glue(
    '  ### Tab1.b2: Proportion plot \n',
    '  tabPanel( \n',
    '    HTML("Proportion plot"), \n',
    '    h4("Proportion / cell numbers across different cell information"), \n',
    '    "In this tab, users can visualise the composition of single cells based on one discrete ", \n',
    '    "cell information across another discrete cell information. ",  \n',
    '    "Usage examples include the library or cellcycle composition across clusters.", \n',
    '    br(),br(), \n',
    '    fluidRow( \n',
    '      column( \n',
    '        3, style="border-right: 2px solid black", \n',
    '        selectInput("{prefix}b2inp1", "Cell information to plot (X-axis):", \n',
    '                    choices = {prefix}conf[grp == TRUE]$UI, \n',
    '                    selected = {prefix}def$grp2) %>%  \n',
    '          helper(type = "inline", size = "m", fade = TRUE, \n',
    '                 title = "Cell information to plot cells by",  \n',
    '                 content = c("Select categorical cell information to plot cells by", \n',
    '                             "- Plotted as the X-axis of the proportion plot")), \n',
    '        selectInput("{prefix}b2inp2", "Cell information to group / colour by:", \n',
    '                    choices = {prefix}conf[grp == TRUE]$UI, \n',
    '                    selected = {prefix}def$grp1) %>%  \n',
    '          helper(type = "inline", size = "m", fade = TRUE, \n',
    '                 title = "Cell information to group / colour cells by", \n',
    '                 content = c("Select categorical cell information to group / colour cells by", \n',
    '                             "- Proportion / cell numbers are shown in different colours")), \n',
    '        radioButtons("{prefix}b2typ", "Plot value:", \n',
    '                     choices = c("Proportion", "CellNumbers"), \n',
    '                     selected = "Proportion", inline = TRUE), \n',
    '        checkboxInput("{prefix}b2flp", "Flip X/Y", value = FALSE), \n',
    '        selectInput("{prefix}b2ord1", "Reorder X-axis by which group:", choices=NULL), \n',
    '        radioButtons("{prefix}b2ord2", "Reorder in which order:", \n',
    '                     choices = c("Decreasing", "Increasing"), \n',
    '                     selected = "Decreasing", inline = TRUE), \n',
    '        actionButton("{prefix}b2togL", "Toggle to subset cells"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}b2togL % 2 == 1", \n',
    '          selectInput("{prefix}b2sub1", "Cell information to subset:", \n',
    '                      choices = {prefix}conf[grp == TRUE]$UI, \n',
    '                      selected = {prefix}def$grp1), \n',
    '          uiOutput("{prefix}b2sub1.ui"), \n',
    '          actionButton("{prefix}b2sub1all", "Select all groups", class = "btn btn-primary"), \n',
    '          actionButton("{prefix}b2sub1non", "Deselect all groups", class = "btn btn-primary") \n',
    '        ), br(), br(), \n',
    '        actionButton("{prefix}b2tog", "Toggle graphics controls"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}b2tog % 2 == 1", \n',
    '          radioButtons("{prefix}b2psz", "Plot size:", \n',
    '                       choices = c("Small", "Medium", "Large"), \n',
    '                       selected = "Medium", inline = TRUE), \n',
    '          radioButtons("{prefix}b2fsz", "Font size:",\n',
    '                       choices = c("Small", "Medium", "Large"),\n',
    '                       selected = "Medium", inline = TRUE),\n',
    '          colourInput("{prefix}b2bg", "Plot background colour:", value = "white")) \n',
    '      ), # End of column (6 space) \n',
    '      column(9, uiOutput("{prefix}b2oup.ui"), \n',
    '             fluidRow(column(2, downloadButton("{prefix}b2oup.dl", "Download Plot")), \n',
    '                      column(2, radioButtons("{prefix}b2oup.f", "Plot format:", \n',
    '                                             choices = c("png", "pdf"), selected = "png", inline = TRUE)), \n',
    '                      column(2, numericInput("{prefix}b2oup.h", "Plot height:", \n',
    '                                             min = 4, max = 20, value = 8, step = 0.5)), \n',
    '                      column(2, numericInput("{prefix}b2oup.w", "Plot width:", \n',
    '                                             min = 4, max = 20, value = 10, step = 0.5))), \n',
    '             br() \n',
    '      )  # End of column (6 space) \n',
    '    )    # End of fluidRow (4 space) \n',
    '  )      # End of tab (2 space) \n',
    '  , \n',
    '  \n'
  )
}

#' Write code for ui.R
#'
#' @rdname wrUImainB3
#' @export wrUImainB3
#'
wrUImainB3 <- function(prefix) {
  glue::glue(
    '  ### Tab1.b3: Bubbleplot / Heatmap \n',
    '  tabPanel( \n',
    '    HTML("Bubbleplot / Heatmap"), \n',
    '    h4("Gene expression bubbleplot / heatmap"), \n',
    '    "In this tab, users can visualise the gene expression patterns of ", \n',
    '    "multiple genes grouped by categorical cell information (e.g. library / cluster).", br(), \n',
    '    "The normalised expression are averaged, log-transformed and then plotted.", \n',
    '    br(),br(), \n',
    '    fluidRow( \n',
    '      column( \n',
    '        3, style="border-right: 2px solid black", \n',
    '        textAreaInput("{prefix}b3inp", HTML("List of gene names <br /> \n',
    '                                          (Max 50 genes, separated <br /> \n',
    '                                           by , or ; or newline):"), \n',
    '                      height = "250px", \n',
    '                      value = paste0({prefix}def$genes[[1]], collapse = ", ")) %>% \n',
    '          helper(type = "inline", size = "m", fade = TRUE, \n',
    '                 title = "List of genes to plot on bubbleplot / heatmap", \n',
    '                 content = c("Input genes to plot", \n',
    '                             "- Maximum 50 genes (due to ploting space limitations)", \n',
    '                             "- Genes should be separated by comma, semicolon or newline")), \n',
    '        selectInput("{prefix}b3ass1", "Assay:", \n',
    '                    choices = {prefix}def$assay, \n',
    '                    selected = {prefix}def$assay[1]), \n',
    '        selectInput("{prefix}b3grp", "Group by:", \n',
    '                    choices = {prefix}conf[grp == TRUE]$UI, \n',
    '                    selected = {prefix}conf[grp == TRUE]$UI[1]) %>% \n',
    '          helper(type = "inline", size = "m", fade = TRUE, \n',
    '                 title = "Cell information to group cells by", \n',
    '                 content = c("Select categorical cell information to group cells by", \n',
    '                             "- Single cells are grouped by this categorical covariate", \n',
    '                             "- Plotted as the X-axis of the bubbleplot / heatmap")), \n',
    '        radioButtons("{prefix}b3plt", "Plot type:", \n',
    '                     choices = c("Bubbleplot", "Heatmap"), \n',
    '                     selected = "Bubbleplot", inline = TRUE), \n',
    '        checkboxInput("{prefix}b3scl", "Scale gene expression", value = TRUE), \n',
    '        checkboxInput("{prefix}b3row", "Cluster rows (genes)", value = TRUE), \n',
    '        checkboxInput("{prefix}b3col", "Cluster columns (samples)", value = FALSE), \n',
    '        sliderInput("{prefix}b3max", "Scale.max:", min=1, max=11, value=3, step=0.1), \n',
    '        checkboxInput("{prefix}b3exp", "expm1 before averaging (check for log-transformed data)",  \n',
    '                      value = TRUE), \n',
    '        br(), \n',
    '        actionButton("{prefix}b3togL", "Toggle to subset cells"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}b3togL % 2 == 1", \n',
    '          selectInput("{prefix}b3sub1", "Cell information to subset:", \n',
    '                      choices = {prefix}conf[grp == TRUE]$UI, \n',
    '                      selected = {prefix}def$grp1), \n',
    '          uiOutput("{prefix}b3sub1.ui"), \n',
    '          actionButton("{prefix}b3sub1all", "Select all groups", class = "btn btn-primary"), \n',
    '          actionButton("{prefix}b3sub1non", "Deselect all groups", class = "btn btn-primary") \n',
    '        ), br(), br(), \n',
    '        actionButton("{prefix}b3tog", "Toggle graphics controls"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}b3tog % 2 == 1", \n',
    '          radioButtons("{prefix}b3cols", "Colour scheme:", \n',
    '                       choices = c("White-Red", "Blue-Yellow-Red", \n',
    '                                   "Yellow-Green-Purple"), \n',
    '                       selected = "Blue-Yellow-Red"), \n',
    '          radioButtons("{prefix}b3psz", "Plot size:", \n',
    '                       choices = c("Small", "Medium", "Large"), \n',
    '                       selected = "Medium", inline = TRUE), \n',
    '          radioButtons("{prefix}b3fsz", "Font size:",\n',
    '                       choices = c("Small", "Medium", "Large"),\n',
    '                       selected = "Medium", inline = TRUE),\n',
    '          colourInput("{prefix}b3bg", "Plot background colour:", value = "white")) \n',
    '      ), # End of column (6 space) \n',
    '      column(9, h4(htmlOutput("{prefix}b3oupTxt")), \n',
    '             uiOutput("{prefix}b3oup.ui"), \n',
    '             fluidRow(column(2, downloadButton("{prefix}b3oup.dl", "Download Plot")), \n',
    '                      column(2, radioButtons("{prefix}b3oup.f", "Plot format:", \n',
    '                                             choices = c("png", "pdf"), selected = "png", inline = TRUE)), \n',
    '                      column(2, numericInput("{prefix}b3oup.h", "Plot height:", \n',
    '                                             min = 4, max = 20, value = 10, step = 0.5)), \n',
    '                      column(2, numericInput("{prefix}b3oup.w", "Plot width:", \n',
    '                                             min = 4, max = 20, value = 10, step = 0.5))), \n',
    '             br() \n',
    '      )  # End of column (6 space) \n',
    '    )    # End of fluidRow (4 space) \n',
    '  ),      # End of tab (2 space) \n',
    '  \n'
  )
}

#' Write code for ui.R - Module Score tab
#'
#' @rdname wrUImainMOD
#' @export wrUImainMOD
#'
wrUImainMOD <- function(prefix) {
  glue::glue(
    '  ### Tab: Module scoring \n',
    '  tabPanel( \n',
    '    HTML("Module scoring"), \n',
    '    h4("Gene Module Score: Violin + Dimred"), \n',
    '    "Compute a per-cell module score (mean expression of a gene list minus the mean of ", \n',
    '    "a random background gene set of equal size), then visualise across cell groups ", \n',
    '    "and on a reduced-dimension plot.", \n',
    '    br(), br(), \n',
    '    fluidRow( \n',
    '      column( \n',
    '        4, \n',
    '        selectInput("{prefix}comb_grp", "Group by:", \n',
    '                    choices = {prefix}conf[grp == TRUE]$UI, \n',
    '                    selected = {prefix}def$grp1) \n',
    '      ), \n',
    '      column( \n',
    '        4, \n',
    '        actionButton("{prefix}comb_togL", "Toggle to subset cells"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}comb_togL % 2 == 1", \n',
    '          selectInput("{prefix}comb_sub1", "Cell information to subset:", \n',
    '                      choices = {prefix}conf[grp == TRUE]$UI, \n',
    '                      selected = {prefix}def$grp1), \n',
    '          uiOutput("{prefix}comb_sub1.ui"), \n',
    '          actionButton("{prefix}comb_sub1all", "Select all groups", class = "btn btn-primary"), \n',
    '          actionButton("{prefix}comb_sub1non", "Deselect all groups", class = "btn btn-primary") \n',
    '        ) \n',
    '      ) \n',
    '    ), \n',
    '    fluidRow( \n',
    '      column( \n',
    '        3, style = "border-right: 2px solid black", \n',
    '        textAreaInput("{prefix}comb_mod_inp", \n',
    '                      HTML("Gene list for module score<br/>(max 50, separated by , ; or newline):"), \n',
    '                      height = "200px", \n',
    '                      value = paste0({prefix}def$genes[[1]], collapse = ", ")) %>% \n',
    '          helper(type = "inline", size = "m", fade = TRUE, \n',
    '                 title = "Genes for module score", \n',
    '                 content = c("Input target genes to score per cell", \n',
    '                             "- Score = mean(target genes) - mean(random background genes)", \n',
    '                             "- Background pool size equals the target gene list size", \n',
    '                             "- Separate genes by comma, semicolon, or newline")), \n',
    '        selectInput("{prefix}comb_mod_ass", "Assay:", \n',
    '                    choices = {prefix}def$assay, \n',
    '                    selected = {prefix}def$assay[1]), \n',
    '        h4(htmlOutput("{prefix}comb_mod_geneTxt")), \n',
    '        br(), \n',
    '        actionButton("{prefix}comb_mod_tog", "Toggle graphics controls"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}comb_mod_tog % 2 == 1", \n',
    '          sliderInput("{prefix}comb_mod_siz", "Point size:", \n',
    '                      min = 0, max = 4, value = 1.25, step = 0.25), \n',
    '          radioButtons("{prefix}comb_mod_psz", "Plot size:", \n',
    '                       choices = c("Small", "Medium", "Large"), \n',
    '                       selected = "Medium", inline = TRUE), \n',
    '          radioButtons("{prefix}comb_mod_fsz", "Font size:", \n',
    '                       choices = c("Small", "Medium", "Large"), \n',
    '                       selected = "Medium", inline = TRUE), \n',
    '          radioButtons("{prefix}comb_mod_col", "Dimred colour palette:", \n',
    '                       choices = c("White-Red", "Blue-Yellow-Red", "Yellow-Green-Purple"), \n',
    '                       selected = "Blue-Yellow-Red"), \n',
    '          radioButtons("{prefix}comb_mod_dr", "Reduction:", \n',
    '                       choices = {prefix}def$dimrd, \n',
    '                       selected = {prefix}def$dimrd[1]), \n',
    '          radioButtons("{prefix}comb_mod_asp", "Aspect ratio:", \n',
    '                       choices = c("Square", "Fixed", "Free"), \n',
    '                       selected = "Square", inline = TRUE), \n',
    '          checkboxInput("{prefix}comb_mod_txt", "Show axis text on dimred", value = FALSE), \n',
    '          radioButtons("{prefix}comb_mod_ord", "Dimred plot order:", \n',
    '                       choices = c("Max-1st", "Min-1st", "Original", "Random"), \n',
    '                       selected = "Max-1st", inline = TRUE), \n',
    '          radioButtons("{prefix}comb_mod_vln_typ", "Violin/boxplot type:", \n',
    '                       choices = c("violin", "boxplot"), \n',
    '                       selected = "violin", inline = TRUE), \n',
    '          checkboxInput("{prefix}comb_mod_vln_pts", "Show data points on violin", value = FALSE), \n',
    '          colourInput("{prefix}comb_mod_bg", "Plot background colour:", value = "white") \n',
    '        ) \n',
    '      ), \n',
    '      column( \n',
    '        5, style = "border-right: 2px solid black", \n',
    '        h4("Module score on reduced dimensions"), \n',
    '        uiOutput("{prefix}comb_mod_dr_oup.ui"), \n',
    '        fluidRow(column(12, uiOutput("{prefix}comb_mod_leg_oup.ui"))), \n',
    '        fluidRow( \n',
    '          column(3, downloadButton("{prefix}comb_mod_dr_oup.dl", "Download")), \n',
    '          column(3, radioButtons("{prefix}comb_mod_dr_oup.f", "Format:", \n',
    '                                 choices = c("png", "pdf"), selected = "png", inline = TRUE)), \n',
    '          column(3, numericInput("{prefix}comb_mod_dr_oup.h", "Height:", \n',
    '                                 min = 4, max = 20, value = 8, step = 0.5)), \n',
    '          column(3, numericInput("{prefix}comb_mod_dr_oup.w", "Width:", \n',
    '                                 min = 4, max = 20, value = 8, step = 0.5)) \n',
    '        ), \n',
    '        br() \n',
    '      ), \n',
    '      column( \n',
    '        4, \n',
    '        h4("Module score violin / boxplot"), \n',
    '        uiOutput("{prefix}comb_mod_vln_oup.ui"), \n',
    '        fluidRow( \n',
    '          column(3, downloadButton("{prefix}comb_mod_vln_oup.dl", "Download")), \n',
    '          column(3, radioButtons("{prefix}comb_mod_vln_oup.f", "Format:", \n',
    '                                 choices = c("png", "pdf"), selected = "png", inline = TRUE)), \n',
    '          column(3, numericInput("{prefix}comb_mod_vln_oup.h", "Height:", \n',
    '                                 min = 4, max = 20, value = 8, step = 0.5)), \n',
    '          column(3, numericInput("{prefix}comb_mod_vln_oup.w", "Width:", \n',
    '                                 min = 4, max = 20, value = 8, step = 0.5)) \n',
    '        ), \n',
    '        br() \n',
    '      ) \n',
    '    )    # End of fluidRow (4 space) \n',
    '  )      # End of tab (2 space) \n',
    '  \n'
  )
}

#' Write code for ui.R: text-only Credits tab
#'
#' @rdname wrUImainCredits
#' @export wrUImainCredits
#'
wrUImainCredits <- function() {
  glue::glue(
    '  ### Tab: Credits \n',
    '  tabPanel( \n',
    '    HTML("Credits"), \n',
    '    h4("Credits"), \n',
    '    "This interactive single-cell explorer was generated with ShinyCell2 and the ", \n',
    '    "OpenOmics cell-seek pipeline.", \n',
    '    br(), br(), \n',
    '    tags$ul( \n',
    '      tags$li( \n',
    '        tags$a(href = "https://github.com/OpenOmics", target = "_blank", \n',
    '               "OpenOmics on GitHub")), \n',
    '      tags$li( \n',
    '        tags$a(href = "https://github.com/OpenOmics/cell-seek", target = "_blank", \n',
    '               "cell-seek pipeline on GitHub")) \n',
    '    ), \n',
    '    br(), \n',
    '    h4("Citation"), \n',
    '    "If you use this resource, please cite the cell-seek pipeline:", \n',
    '    br(), br(), \n',
    '    tags$blockquote( \n',
    '      "Vicky Chen, & Skyler Kuhn. (2023). OpenOmics/cell-seek [Computer software]. ", \n',
    '      tags$a(href = "https://doi.org/10.5281/zenodo.10181524", target = "_blank", \n',
    '             "https://doi.org/10.5281/zenodo.10181524")\n',
    '  )\n',
    '  )      # End of tab (2 space) \n',
    '  \n'
  )
}

#' Write code for ui.R
#'
#' @rdname wrUImainS1
#' @export wrUImainS1
#'
wrUImainS1 <- function(prefix, ptsiz, hasMultiSlides = FALSE) {
  # Calculate dynamic slider parameters based on cell count
  # Read metadata to get cell count
  meta_file <- paste0(prefix, "meta.rds")
  if (file.exists(meta_file)) {
    meta <- readRDS(meta_file)
    n_cells <- nrow(meta)
  } else {
    n_cells <- 10000  # Default fallback
  }
  
  # Use heuristic: >20k cells = fine control, <20k = broad control
  if (n_cells > 20000) {
    # Fine control for large datasets
    slider_min <- 0.1
    slider_max <- 2
    slider_step <- 0.05
  } else {
    # Broad control for smaller datasets
    slider_min <- 0.25
    slider_max <- 2
    slider_step <- 0.25
  }
  
  # Ensure ptsiz is within valid range
  ptsiz_val <- max(slider_min, min(as.numeric(ptsiz), slider_max))
  
  slide_input_ui <- ""
  if (hasMultiSlides) {
    slide_input_ui <- glue::glue(
      '        selectInput("{prefix}s1slide", "Select Slide:",\n',
      '                    choices = setNames({prefix}image$slide_names, {prefix}image$slide_display_names),\n',
      '                    selected = {prefix}image$slide_names[1]),\n',
      '        br(),\n'
    )
  }
  
  glue::glue(
    '  ### Tab1.s1: Zoom-enable spatial \n',
    '  tabPanel( \n',
    '    HTML("Zoom<span class=\'nav-sub\'>Spatial</span>"), \n',
    '    h4("Zoom-enable spatial plot overlaid with cell info or assay expression"), \n',
    '    "In this tab, users can visualise either cell information and gene expression ",  \n',
    '    "in a zoom-enabled spatial plot.", \n',
    '    br(),br(), \n',
    '    fluidRow( \n',
    '      column( \n',
    '        3, fluidRow(\n',
    '          column(\n',
    '            12, sliderInput("{prefix}s1alp", "Dot transparency: ", \n',
    '                            min = 0, max = 1, value = 1, step = 0.05))\n',
    '        ) \n',
    '      ), # End of column (6 space) \n',
    '      column( \n',
    '        3, actionButton("{prefix}s1togL", "Toggle to subset cells"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}s1togL % 2 == 1", \n',
    '          selectInput("{prefix}s1sub1", "Cell information to subset:", \n',
    '                      choices = {prefix}conf[grp == TRUE]$UI, \n',
    '                      selected = {prefix}def$grp1), \n',
    '          uiOutput("{prefix}s1sub1.ui"), \n',
    '          actionButton("{prefix}s1sub1all", "Select all groups", class = "btn btn-primary"), \n',
    '          actionButton("{prefix}s1sub1non", "Deselect all groups", class = "btn btn-primary") \n',
    '        ) \n',
    '      ), # End of column (6 space) \n',
    '      column( \n',
    '        6, actionButton("{prefix}s1tog0", "Toggle graphics controls"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}s1tog0 % 2 == 1", \n',
    '          fluidRow( \n',
    '            column( \n',
    '              6, sliderInput("{prefix}s1siz", "Point size:", \n',
    '                             min = {slider_min}, max = {slider_max}, value = {ptsiz_val}, step = {slider_step}), \n',
    '              radioButtons("{prefix}s1psz", "Plot size:", \n',
    '                           choices = c("Small", "Medium", "Large"), \n',
    '                           selected = "Medium", inline = TRUE), \n',
    '              radioButtons("{prefix}s1fsz", "Font size:", \n',
    '                           choices = c("Small", "Medium", "Large"), \n',
    '                           selected = "Medium", inline = TRUE), \n',
    '              colourInput("{prefix}s1bg", "Plot background colour:", value = "white") \n',
    '            ) \n',
    '          ) \n',
    '        ) \n',
    '      )  # End of column (6 space) \n',
    '    ),   # End of fluidRow (4 space) \n',
    '    fluidRow( \n',
    '      column( \n',
    '        3, style="border-right: 2px solid black", h4("Information to plot"),\n',
    slide_input_ui,
    '        selectInput("{prefix}s1inp1", "Cell metadata to colour plot:", choices = {prefix}conf$UI, selected = {prefix}def$meta1) %>% \n',
    '          helper(type = "inline", size = "m", fade = TRUE,\n',
    '                 title = "Cell Info / Gene to colour cells by",\n',
    '                 content = c("Select cell info / feature to colour cells",\n',
    '                             "- Categorical covariates have a fixed colour palette",\n',
    '                             paste0("- Continuous covariates / gene expression are coloured ",\n',
    '                                    "in a Blue-Yellow-Red colour scheme, which can be ",\n',
    '                                    "changed in the plot controls"))),\n',
    '        strong("Draw box on plot below to zoom (double-click to reset zoom): "), \n',
    '        plotOutput("{prefix}s1oup1.br", height = "400px",\n',
    '                   brush = brushOpts(id = "{prefix}s1inp1.br", resetOnNew = TRUE),\n',
    '                   dblclick = "{prefix}s1inp1.br_dblclick"), \n',
    '        actionButton("{prefix}s1tog1", "Toggle plot controls"), \n',
    '        conditionalPanel(\n',
    '          condition = "input.{prefix}s1tog1 % 2 == 1",\n',
    '          radioButtons("{prefix}s1col1", "Colour (Continuous data):",\n',
    '                       choices = c("White-Red","Blue-Yellow-Red","Yellow-Green-Purple"),\n',
    '                       selected = "Blue-Yellow-Red"),\n',
    '          numericInput("{prefix}s1min1", "Min cutoff (q##):",\n',
    '                       min = 0, max = 50, value = 0, step = 1),\n',
    '          numericInput("{prefix}s1max1", "Max cutoff (q##):",\n',
    '                       min = 50, max = 100, value = 100, step = 1),\n',
    '          checkboxInput("{prefix}s1lab1", "Show cell info labels", value = TRUE)\n',
    '        )\n',
    '      ), # End of column (6 space) \n',
    '      column(\n',
    '        5, style="border-right: 2px solid black",\n',
    '        fluidRow(column(12, uiOutput("{prefix}s1oup1.ui"))),\n',
    '        fluidRow(column(12, uiOutput("{prefix}s1oup3.ui"))),\n',
    '        fluidRow(column(3, downloadButton("{prefix}s1oup1.dl", "Download Plot")),\n',
    '                 column(3, radioButtons("{prefix}s1oup1.f", "Plot format:",\n',
    '                                        choices = c("png", "pdf"), selected = "png", inline = TRUE)),\n',
    '                 column(3, numericInput("{prefix}s1oup1.h", "Plot height:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5)),\n',
    '                 column(3, numericInput("{prefix}s1oup1.w", "Plot width:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5))),\n',
    '        fluidRow(column(3, downloadButton("{prefix}s1oup3.dl", "Download Legend")),\n',
    '                 column(3, radioButtons("{prefix}s1oup3.f", "Legend format:",\n',
    '                                        choices = c("png", "pdf"), selected = "png", inline = TRUE)),\n',
    '                 column(3, numericInput("{prefix}s1oup3.h", "Legend height:",\n',
    '                                        min = 0.2, max = 10, value = 2, step = 0.1)),\n',
    '                 column(3, numericInput("{prefix}s1oup3.w", "Legend width:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5))),\n',
    '        br()\n',
    '      ), # End of column (5 space)\n',
    '      column(\n',
    '        4, h4("Cell numbers"),\n',
    '        numericInput("{prefix}s1splt", "Split continuous cell info into nBins:",\n',
    '                     min = 2, max = 10, value = 4, step = 1),\n',
    '        dataTableOutput("{prefix}s1.dt")\n',
    '      )  # End of column (4 space)\n',
    '    )    # End of fluidRow (4 space) \n',
    '  )      # End of tab (2 space) \n',
    '  , \n',
    '  \n'
  )
}

#' Write code for ui.R
#'
#' @rdname wrUImainS2
#' @export wrUImainS2
#'
wrUImainS2 <- function(prefix, ptsiz, hasMultiSlides = FALSE) {
  # Calculate dynamic slider parameters based on cell count
  # Read metadata to get cell count
  meta_file <- paste0(prefix, "meta.rds")
  if (file.exists(meta_file)) {
    meta <- readRDS(meta_file)
    n_cells <- nrow(meta)
  } else {
    n_cells <- 10000  # Default fallback
  }
  
  # Use heuristic: >20k cells = fine control, <20k = broad control
  if (n_cells > 20000) {
    # Fine control for large datasets
    slider_min <- 0.1
    slider_max <- 2
    slider_step <- 0.05
  } else {
    # Broad control for smaller datasets
    slider_min <- 0.25
    slider_max <- 2
    slider_step <- 0.25
  }
  
  # Ensure ptsiz is within valid range
  ptsiz_val <- max(slider_min, min(as.numeric(ptsiz), slider_max))
  
  slide_input_ui <- ""
  # Spacer matching the left column's "Select Slide:" selector (only present for
  # multi-slide apps), so the right plot lines up with the left plot. Empty when
  # single-slide, since neither column then has a slide selector.
  slide_spacer_ui <- ""
  if (hasMultiSlides) {
    slide_input_ui <- glue::glue(
      '        selectInput("{prefix}s2slide", "Select Slide:",\n',
      '                    choices = setNames({prefix}image$slide_names, {prefix}image$slide_display_names),\n',
      '                    selected = {prefix}image$slide_names[1]),\n'
    )
    slide_spacer_ui <- glue::glue(
      '        div(style = "visibility: hidden;", `aria-hidden` = "true",\n',
      '            selectInput("{prefix}s2slide_spacer", "Select Slide:",\n',
      '                        choices = "", selected = "")),\n'
    )
  }

  glue::glue(
    '  ### Tab1.s2: CellInfo vs AssayExpr on spatial \n',
    '  tabPanel( \n',
    '    HTML("Side-by-side<span class=\'nav-sub\'>Spatial</span>"), \n',
    '    h4("Cell information vs assay expression on spatial coordinates"), \n',
    '    "In this tab, users can visualise both cell information and gene ",  \n',
    '    "expression side-by-side on spatial plot.", \n',
    '    br(),br(), \n',
    '    fluidRow( \n',
    '      column( \n',
    '        3, fluidRow(\n',
    '          column(\n',
    '            12, sliderInput("{prefix}s2alp", "Dot transparency: ", \n',
    '                            min = 0, max = 1, value = 1, step = 0.05)) \n',
    '        ) \n',
    '      ), # End of column (6 space) \n',
    '      column( \n',
    '        3, actionButton("{prefix}s2togL", "Toggle to subset cells"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}s2togL % 2 == 1", \n',
    '          selectInput("{prefix}s2sub1", "Cell information to subset:", \n',
    '                      choices = {prefix}conf[grp == TRUE]$UI, \n',
    '                      selected = {prefix}def$grp1), \n',
    '          uiOutput("{prefix}s2sub1.ui"), \n',
    '          actionButton("{prefix}s2sub1all", "Select all groups", class = "btn btn-primary"), \n',
    '          actionButton("{prefix}s2sub1non", "Deselect all groups", class = "btn btn-primary") \n',
    '        ) \n',
    '      ), # End of column (6 space) \n',
    '      column( \n',
    '        6, actionButton("{prefix}s2tog0", "Toggle graphics controls"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}s2tog0 % 2 == 1", \n',
    '          fluidRow( \n',
    '            column( \n',
    '              6, sliderInput("{prefix}s2siz", "Point size:", \n',
    '                             min = {slider_min}, max = {slider_max}, value = {ptsiz_val}, step = {slider_step}), \n',
    '              radioButtons("{prefix}s2psz", "Plot size:", \n',
    '                           choices = c("Small", "Medium", "Large"), \n',
    '                           selected = "Medium", inline = TRUE), \n',
    '              radioButtons("{prefix}s2fsz", "Font size:", \n',
    '                           choices = c("Small", "Medium", "Large"), \n',
    '                           selected = "Medium", inline = TRUE), \n',
    '              colourInput("{prefix}s2bg", "Plot background colour:", value = "white") \n',
    '            ) \n',
    '          ) \n',
    '        ) \n',
    '      )  # End of column (6 space) \n',
    '    ),   # End of fluidRow (4 space) \n',
    '    fluidRow( \n',
    '      column(\n',
    '        6, style="border-right: 2px solid black",\n',
    slide_input_ui,
    '        fluidRow(\n',
    '          column(\n',
    '            6, selectInput("{prefix}s2inp1", "Cell metadata to colour plot:", choices = {prefix}conf$UI, selected = {prefix}def$meta1) %>% \n',
    '              helper(type = "inline", size = "m", fade = TRUE,\n',
    '                     title = "Cell metadata to colour cells by",\n',
    '                     content = c("Select cell metadata to colour cells",\n',
    '                                 "- Categorical covariates have a fixed colour palette",\n',
    '                                 paste0("- Continuous covariates are coloured ",\n',
    '                                        "in a Blue-Yellow-Red colour scheme, which can be ",\n',
    '                                        "changed in the plot controls")))\n',
    '          ),\n',
    '          column(\n',
    '            6, actionButton("{prefix}s2tog1", "Toggle plot controls"),\n',
    '            conditionalPanel(\n',
    '              condition = "input.{prefix}s2tog1 % 2 == 1",\n',
    '              radioButtons("{prefix}s2col1", "Colour (Continuous data):",\n',
    '                           choices = c("White-Red","Blue-Yellow-Red","Yellow-Green-Purple"),\n',
    '                           selected = "Blue-Yellow-Red"),\n',
    '              numericInput("{prefix}s2min1", "Min cutoff (q##):",\n',
    '                           min = 0, max = 50, value = 0, step = 1),\n',
    '              numericInput("{prefix}s2max1", "Max cutoff (q##):",\n',
    '                           min = 50, max = 100, value = 100, step = 1),\n',
    '              checkboxInput("{prefix}s2lab1", "Show cell info labels", value = TRUE)\n',
    '            )\n',
    '          )\n',
    '        ),\n',
    '        fluidRow(column(12, uiOutput("{prefix}s2oup1.ui"))),\n',
    '        fluidRow(column(12, uiOutput("{prefix}s2oup3.ui"))),\n',
    '        fluidRow(column(3, downloadButton("{prefix}s2oup1.dl", "Download Plot")),\n',
    '                 column(3, radioButtons("{prefix}s2oup1.f", "Plot format:", \n',
    '                                        choices = c("png", "pdf"), selected = "png", inline = TRUE)),\n',
    '                 column(3, numericInput("{prefix}s2oup1.h", "Plot height:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5)),\n',
    '                 column(3, numericInput("{prefix}s2oup1.w", "Plot width:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5))),\n',
    '        fluidRow(column(3, downloadButton("{prefix}s2oup3.dl", "Download Legend")),\n',
    '                 column(3, radioButtons("{prefix}s2oup3.f", "Legend format:", \n',
    '                                        choices = c("png", "pdf"), selected = "png", inline = TRUE)),\n',
    '                 column(3, numericInput("{prefix}s2oup3.h", "Legend height:",\n',
    '                                        min = 0.2, max = 10, value = 2, step = 0.1)),\n',
    '                 column(3, numericInput("{prefix}s2oup3.w", "Legend width:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5))),\n',
    '        br()\n',
    '      ), # End of column (6 space)\n',
    '      column( \n',
    '        6,\n',
    slide_spacer_ui,
    '        fluidRow( \n',
    '          column(\n',
    '            6, selectInput("{prefix}s2inp2", "Cell metadata to colour plot:", choices = {prefix}conf$UI, selected = {prefix}def$meta2) %>% \n',
    '              helper(type = "inline", size = "m", fade = TRUE,\n',
    '                     title = "Cell metadata to colour cells by",\n',
    '                     content = c("Select cell metadata to colour cells",\n',
    '                                 "- Categorical covariates have a fixed colour palette",\n',
    '                                 paste0("- Continuous covariates are coloured ",\n',
    '                                        "in a Blue-Yellow-Red colour scheme, which can be ",\n',
    '                                        "changed in the plot controls")))\n',
    '          ),\n',
    '          column(\n',
    '            6, actionButton("{prefix}s2tog2", "Toggle plot controls"),\n',
    '            conditionalPanel(\n',
    '              condition = "input.{prefix}s2tog2 % 2 == 1",\n',
    '              radioButtons("{prefix}s2col2", "Colour:",\n',
    '                           choices = c("White-Red","Blue-Yellow-Red","Yellow-Green-Purple"),\n',
    '                           selected = "White-Red"),\n',
    '              numericInput("{prefix}s2min2", "Min cutoff (q##):",\n',
    '                           min = 0, max = 50, value = 0, step = 1),\n',
    '              numericInput("{prefix}s2max2", "Max cutoff (q##):",\n',
    '                           min = 50, max = 100, value = 100, step = 1),\n',
    '              checkboxInput("{prefix}s2lab2", "Show cell info labels", value = TRUE)\n',
    '            )\n',
    '          )\n',
    '        ) , \n',
    '        fluidRow(column(12, uiOutput("{prefix}s2oup2.ui"))),\n',
    '        fluidRow(column(12, uiOutput("{prefix}s2oup4.ui"))),\n',
    '        fluidRow(column(3, downloadButton("{prefix}s2oup2.dl", "Download Plot")),\n',
    '                 column(3, radioButtons("{prefix}s2oup2.f", "Plot format:", \n',
    '                                        choices = c("png", "pdf"), selected = "png", inline = TRUE)),\n',
    '                 column(3, numericInput("{prefix}s2oup2.h", "Plot height:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5)),\n',
    '                 column(3, numericInput("{prefix}s2oup2.w", "Plot width:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5))),\n',
    '        fluidRow(column(3, downloadButton("{prefix}s2oup4.dl", "Download Legend")),\n',
    '                 column(3, radioButtons("{prefix}s2oup4.f", "Legend format:", \n',
    '                                        choices = c("png", "pdf"), selected = "png", inline = TRUE)),\n',
    '                 column(3, numericInput("{prefix}s2oup4.h", "Legend height:",\n',
    '                                        min = 0.2, max = 10, value = 2, step = 0.1)),\n',
    '                 column(3, numericInput("{prefix}s2oup4.w", "Legend width:",\n',
    '                                        min = 4, max = 20, value = 8, step = 0.5))),\n',
    '        br()\n',
    '      )  # End of column (6 space) \n',
    '    )    # End of fluidRow (4 space) \n',
    '  )      # End of tab (2 space) \n',
    '  , \n',
    '  \n'
  )
}

#' Write code for ui.R
#'
#' @rdname wrUImainT1
#' @export wrUImainT1
#'
wrUImainT1 <- function(prefix) {
  glue::glue(
    '  ### Tab1.t1: Trackplot \n',
    '  tabPanel( \n',
    '    HTML("Track plot"), \n',
    '    h4("Track plot"), \n',
    '    "In this tab, users can visualise open chromatin regions.", \n',
    '    br(),br(), \n',
    '    fluidRow( \n',
    '      column( \n',
    '        3, style="border-right: 2px solid black", \n',
    '        radioButtons("{prefix}t1sel", "Input method:", choices = c("Gene", "Region"), \n',
    '                     selected = "Region", inline = TRUE),\n',
    '        conditionalPanel(\n',
    '          condition = "input.{prefix}t1sel == \'Gene\'",\n',
    '          selectInput("{prefix}t1gene", "Gene:",\n',
    '                      choices = NULL)),\n',
    '        conditionalPanel(\n',
    '          condition = "input.{prefix}t1sel == \'Region\'",\n',
    '          selectInput("{prefix}t1chr", "Chromosome:",\n',
    '                      choices = {prefix}bw$chrSize$chr, \n',
    '                      selected = {prefix}bw$chrGene[42]$chr), \n',
    '          fluidRow(column(6, textInput("{prefix}t1srt", "Start", value = {prefix}bw$chrGene[42]$srt)),\n',
    '                   column(6, textInput("{prefix}t1end", "End",   value = {prefix}bw$chrGene[42]$end)))\n',
    '        ),\n',
    '        selectInput("{prefix}t1grp", "Group by:",\n',
    '                    choices = {prefix}bwGrp,\n',
    '                    selected = {prefix}bwGrp[1]),\n',
    '        selectInput("{prefix}t1bed", "Track annotation bedfile:", choices = NULL),\n',
    '        fluidRow(column(6, numericInput("{prefix}t1extL", "extend left by:",\n',
    '                                        min = 0, max = 50000, value = 5000, step = 1000)),\n',
    '                 column(6, numericInput("{prefix}t1extR", "extend right by:",\n',
    '                                        min = 0, max = 50000, value = 5000, step = 1000))),\n',
    '        checkboxInput("{prefix}t1col", \n',
    '                      label = "Check to color track plot:", \n',
    '                      value = TRUE), # FALSE means "gray" is selected by default\n',
    '        actionButton("{prefix}t1tog1", "Toggle to subset cells"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}t1tog1 % 2 == 1", \n',
    '          uiOutput("{prefix}t1sub1.ui"),\n',
    '          actionButton("{prefix}t1sub1all", "Select all groups", class = "btn btn-primary"), \n',
    '          actionButton("{prefix}t1sub1non", "Deselect all groups", class = "btn btn-primary") \n',
    '        ), \n',
    '        div(style = "display:inline-block; float:right", actionButton("{prefix}t1update", "Update View")),\n',
    '        br(), br(),\n',
    '        fluidRow(column(6, numericInput("{prefix}t1track.h", "plot height (pixels):",\n',
    '                                        min = 400, max = 2000, value = 600, step = 50)),\n',
    '                 column(6, numericInput("{prefix}t1track.w", "plot width (pixels):",\n',
    '                                        min = 400, max = 2000, value = 1000, step = 50)))\n',
    '      ), \n',
    '      column(\n',
    '        9, actionButton("{prefix}t1tog0", "Toggle graphics controls"), \n',
    '        conditionalPanel( \n',
    '          condition = "input.{prefix}t1tog0 % 2 == 1", \n',
    '          fluidRow( \n',
    '            column(4, \n',
    '                   checkboxInput("{prefix}t1dgt", "Display gene track", value = TRUE),\n',
    '                   checkboxInput("{prefix}t1asg", "Auto scale group", value = TRUE),\n',
    '                   checkboxInput("{prefix}t1sha", "Show axis", value = TRUE),\n',
    '                   checkboxInput("{prefix}t1ctx", "Collapse transcripts", value = TRUE)\n',
    '            ), \n',
    '            column(4,\n',
    '                   sliderInput("{prefix}t1ath", "Annotation track height: ", \n',
    '                               min = 1, max = 10, value = 2, step = 1),\n',
    '                   sliderInput("{prefix}t1gth", "Gene track height: ", \n',
    '                               min = 1, max = 10, value = 4, step = 1)\n',
    '            ),\n',
    '            column(4,\n',
    '                   sliderInput("{prefix}t1tnp", "Track names position: ", \n',
    '                               min = 0, max = 1, value = 0, step = 0.1),\n',
    '                   sliderInput("{prefix}t1gsz", "Font size: ", \n',
    '                               min = 0, max = 3, value = 2, step = 0.25) \n',
    '            ))),\n',
    '        uiOutput("{prefix}t1track.ui")\n',
    '      )  # End of column (6 space) \n',
    '    )    # End of fluidRow (4 space) \n',
    '  )      # End of tab (2 space) \n',
    '  , \n',
    '  \n'
  )
}

#' Write code for ui.R
#' 
#' @param footnote shiny app footnote
#'
#' @rdname wrUIpost
#' @export wrUIpost
#'
wrUIpost <- function(footnote) {
  if(is.list(footnote)){
    f1 = ''; f2 = ''; f3 = ''; f4 = ''; f5 = ''; f6 = ''; f7 = ''; f8 = ''
    if(!is.null(footnote$author)){ f1 = paste0('"',footnote$author,' ",')}
    if(!is.null(footnote$title)){  f2 = paste0('"',footnote$title,' ",')}
    if(!is.null(footnote$journal)){f3 = paste0('em("',footnote$journal,' "),')}
    if(!is.null(footnote$volume)){ f4 = paste0('strong("',footnote$volume,', "),')}
    if(!is.null(footnote$page)){   f5 = paste0('"',footnote$page,' ",')}
    if(!is.null(footnote$year)){   f6 = paste0('"(',footnote$year,') ",')}
    if(!is.null(footnote$doi)){    f7 = paste0('"doi: ',footnote$doi,' ",')}
    if(!is.null(footnote$link)){   f8 = paste0('a("[Link]", href = "',footnote$link,'", target="_blank"),')}
    f0 = paste0('strong("Reference: "),', f1, f2, f3, f4, f5, f6, f7, f8, 
                'style = "font-size: 125%;"')
  } else {
    f0 = paste0('"', footnote[[1]], '", style = "font-size: 125%;"')
  }
  
  glue::glue(
    '  ),\n',
    '  hr(),\n',
    '  br()\n',
    '))\n'
  )
}

#' Write code for google-analytics.html
#'
#' @param gaID Google analytics tracking ID (e.g. "UA-123456789-0")
#' 
#' @rdname wrUIga
#' @export wrUIga
#'
wrUIga <- function(gaID) {
  glue::glue('<!-- Global site tag (gtag.js) - Google Analytics --> \n',
             '  <script async src="https://www.googletagmanager.com/gtag/js?id={gaID}"></script> \n',
             '  <script> \n',
             '  window.dataLayer = window.dataLayer || []; \n',
             'function gtag(){{dataLayer.push(arguments);}} \n',
             'gtag(\'js\', new Date()); \n',
             ' \n',
             'gtag(\'config\', \'{gaID}\'); \n',
             '</script> \n')
}
